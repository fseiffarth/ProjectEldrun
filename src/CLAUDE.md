# ProjectEldrun — Frontend File Map (`src/`)

Only the load-bearing files are listed; the tree is the source of truth. See
the root `AGENTS.md` for project-wide context (running, persistence, dev
workflow); see `src-tauri/CLAUDE.md` for the backend file map.

**Entry & shell**

| File | Purpose |
|------|---------|
| `src/App.tsx` | Root component, theme injection, global key handlers. |
| `src/main.tsx` | React entry point. |
| `src/crashReporter.ts` | Captures/forwards WebKitGTK renderer crashes to the backend. |
| `src/types/index.ts` | Shared TypeScript types. |

**Layout (`src/components/layout/`)**

| File | Purpose |
|------|---------|
| `AppShell.tsx` | Top-level layout: header, center, right-panel wiring. |
| `HeaderBar.tsx` | Window drag handle + hosts the project switcher in the header. |
| `GlobalAppBar.tsx` | Global toolbar / app launcher (`GLOBAL_APP_ROLES`). |
| `GlobalAppMenu.tsx` | Context menu for a global-app toolbar button. |
| `CenterPanel.tsx` | Tab/subwindow tiling host; keeps all panes mounted across scope switches. |
| `DetachedCenterPanel.tsx` | Center-panel variant rendered inside a detached OS window. |
| `DetachedApp.tsx` | Root component for a popped-out/detached subwindow (#42). Also holds the **no-fullscreen guard**, which is what keeps a popout movable at all: a window in `_NET_WM_STATE_FULLSCREEN` loses `_NET_WM_ACTION_MOVE`, so the WM refuses the `_NET_WM_MOVERESIZE` that `startDragging` sends and both the titlebar drag and every tab bar's grip silently no-op (observed under Muffin; `restore_main_window` documents the same under KWin). A popout has no OS title bar, so nothing on screen tells a fullscreen one from a merely large one — it just stops being movable. Hence F11 **maximizes** here (`DetachedCenterPanel`) rather than fullscreening, and this component clears a stray fullscreen on mount as the net under that. macOS is excluded, the exclusion `restore_main_window` makes. |
| `ProjectSwitcher.tsx` | Thin composition root: pill strip + search/dialog/settings wiring. Re-exports scaffold helpers. |
| `ProjectSearch.tsx` *(in `projects/`)* | Inactive-project/box search box + results popover. |
| `ProjectDialog.tsx` *(in `projects/`)* | New/Import project dialog incl. SSH + OpenVPN + scaffold-fill sub-flows. An import's source is a local folder, a GitHub/GitLab **clone** (`git_clone` lands the tree, then the ordinary import registers it in place), or a **fork** of one (`git_fork_clone` — the same landing, preceded by creating the user's own copy on the host so the project has something to push to, with the original left as `upstream`). Also asks the **project-container** question here rather than only in the pill menu, because this is the one moment it is free (flipping it later restarts every tab): the row defaults **on for an import** — folder, clone or fork, i.e. code nobody has read yet — and **off for a new project**, with an explicit choice sticking; it runs the pill's own `set_project_sandbox` + preflight before the project reaches the store, so activation warms the right container. Fork adds only a *Host type* row (detect-from-URL / GitHub / GitLab: a self-hosted host names neither, and the provider decides which CLI runs) and a `gh`/`glab` install banner; every other branch is the clone's, since a fork import *is* a clone import. Also **pre-checks whether the site is already a project** (#152) as the folder/host path is picked, via `check_project_site` — the *same* resolver the backend refuses with, so the warning and the refusal cannot disagree. It is advisory (the list can move under it) and the write-side gate is the real one, but it is what stops a clone or fork from downloading a whole repository into a destination that is about to be rejected; a missing command resolves to "no conflict" rather than blocking a dialog against an older backend. The notice names the colliding project and offers to **open it**, because a dead-end error would leave the user to go find it. The site it checks mirrors the backend's: the host path when remote, else the folder the project ends up in — which for a *copy* import is the destination, not the source it copies from. The container row grew into the **three-way trust tier** (Local / Container / VM, `docs/vm_projects_plan.md`): the VM option is offered only when `vm_doctor` passes and only for a new project or a plain clone import (a folder/fork import would need a host→VM data move v1 doesn't do); an import *recommends* the VM when available, falling back to the container as before. A VM submit skips the local create path entirely — `create_project` with a `vm` spec, boot, then for a clone a **visible tab running `git clone` inside the VM** through a temporary proxy allow, so the untrusted bytes never land on the host. |
| `SettingsPanel.tsx` | Settings dialog + sub-panels (theme/git/layout, global apps, file-type apps, Ollama, shortcuts, help). Built entirely out of `settingsUi.tsx` — do not hand-roll a row, a card or a header here. |
| `settingsUi.tsx` | The **settings design system** in components: `SettingsHeader` / `SettingsSection` / `SettingsCard` / `SettingRow` / `ToggleRow` / `ToggleCard` / `SettingsList`, with the matching CSS under "Settings design system" in `styles/themes.css`. Every settings surface — this dialog, its sub-panels, `UpdatesPanel`, `MobileSettings`, and the settings-shaped sections other dialogs embed — is made of these and nothing else, because the alternative is what was there before: bordered cards alternating with bare rows, so half the controls sat inside a card edge and half sat flush with the dialog padding; each `Dropdown` sized to its own longest option, so no two shared a right edge; `.settings-row input` matching **no rule at all**, so several fields rendered as raw native inputs; and help paragraphs floating between rows, belonging to nothing. Two rules keep it that way: help text is a control's `help` prop (a loose `<p className="settings-help">` is a section intro and nothing else), and a button says `settings-btn` (+ `sm`/`icon`/`primary`/`danger`) — the bare-element selectors that style an unclassed button are legacy and only cover a few specific rows. |
| `UpdatesPanel.tsx` | **Settings → Updates** (Group H #209b): is there a newer Eldrun on the project's GitHub releases page, and install it. It checks **on mount**, which is the one place an automatic request is honest — the user navigated to a screen whose entire subject is that question; nothing else here polls and Eldrun never checks in the background. The three-step shape (check → download → install) is deliberate rather than one button: the middle step takes minutes on a 150 MB artifact and the last one closes the app on two of the three platforms, so the user who has just read what changed is the one who decides when. **No path and no URL is ever sent** — `download_app_update` and `install_app_update` take nothing, the backend re-checks and installs what it staged. The release body renders as the plain text it is; no markdown parser is run over text fetched from the network. A `manual` install (a `.deb` or package-managed copy) is offered no install button at all, only where the file went, and **no branch here relaunches Eldrun**: a window holds live terminals and open tabs. Tested in `src/__tests__/UpdatesPanel.test.tsx`; untested live. |
| `mobile/MobileSettings.tsx` + `mobile/MobileBridgeHost.tsx` | Eldrun Mobile desktop surface (`docs/eldrun_mobile_agent_plan.md`): host/Serve/pairing/device/project controls plus the narrow event bridge that catalogs permitted agents, idempotently creates typed shell or resumable-agent tabs, and supplies a display-only desktop Alerts snapshot. It never exposes paths, argv, or raw source ids to the phone. |
| `RightPanel.tsx` | File-tree overlay panel. A thin host: its whole file *viewer* is the shared `files/ProjectFilesView` (same component the Files (Project) tab renders). It owns only what is panel-specific — the active-project identity it forwards, the browsed-folder in the projects store, and the three window-chrome fragments it injects as slots (📌 pin, the resize border, the "Hidden subwindows" list). It also opens in the **root scope**, rooted at `~/eldrun/root` (`ROOT_SCOPE`, `stores/projects`' `rootDir`) — the folder that belongs to no project, and therefore the app's staging area for data that is only being looked at or has not found a project yet. That folder had a control terminal and no file view, so the one way to see what was in it was `ls`; with the panel it gets the whole viewer, which is what makes it usable as one — the tree, drag-and-drop, the 📥 Downloads section (drop a download straight into it) and the tree's own "Send to project…" for the moment a file does find a home. Deliberately keyed off the **scope**, not merely "no active project": a box scope has none either and must keep its own multi-root view. Two things a project has are absent by construction rather than by a special case: no ⚙ project settings (`projectId` is null — there is no `project.json` to write), and no persistence of the browsed folder (`setRightPanelFolder` saves through the owning project's `project.json`), so the root's folder is session-only. `AppShell`'s `panelTarget` is what admits the scope at all. |
| `VpnPasswordPrompt.tsx` | Modal prompting for an OpenVPN password on activation. |
| `LogoIcon.tsx` | Inline SVG logo (full ring/branches + spark), currently unused by the header — kept as the source design in case a full mark is wanted elsewhere. |
| `StarIcon.tsx` | The logo's gold spark alone, cropped to its own viewBox. Renders in exactly two places, and the pair is the rule: the header's root-terminal button (the main window's mark) and a **detached** window's own title strip (`DetachedCenterPanel`'s frame). It was briefly on every subwindow's `tabs/TabBar.tsx` too and is not any more — a mark repeated once per tab group is decoration, whereas on a popout it is the only identity an undecorated OS window has. |

**Projects, header widgets, tabs, terminal, files, embed, common**

| File | Purpose |
|------|---------|
| `projects/ProjectPill.tsx` | Individual project pill (click/close/drag-reorder/group). Its "Remote machines…" Runtime item opens `RemoteMachinesWindow`. A VM project (`project.vm?.enabled`) additionally shows an always-visible ▣/▢ state glyph beside the lamps (the lamps say "connected"; only this says whether the machine itself is up) and a "VM settings…" Runtime item — both open `VmSettingsDialog`. |
| `projects/VmSettingsDialog.tsx` | The VM tier's knobs dialog (`docs/vm_projects_plan.md`, `docs/context/vm_projects.md`): running/off status + Boot/Shut down/Rebuild (the rebuild confirm names the overlay as the working tree — for a mirrorless VM project it IS the only copy), memory/cpus/disk (next-boot), the three-mode egress knob with the honest proxy caveat stated in place, the GitHub opt-in + extra-hosts allowlist (live-applied), and the blocked-CONNECT log (the exfiltration tripwire, polled while open). Chrome copied from `ContainerSettingsWindow`, its structural sibling. Untested. |
| `projects/HpcPipelineWizard.tsx` | **The guided HPC/SLURM pipeline wizard** (`docs/quirky-knitting-umbrella` plan Phases B + C), launched from the project-switcher **+** menu ("HPC pipeline…"). A 6-step stepper that **composes existing flows** rather than reimplementing them: **Login** (`RemoteProjectSection` + `useRemoteSession`) → **Project** (name + local-mirror location) → **Workspace** (`lib/hpcWorkspace`: allocate or adopt an `hpc-workspace`, **then** `create_project`) → **Load data** (skippable; picks local files → `remote_upload_file` streams them to the host over the pooled SFTP) → **Run job** (edits a starter `.slurm`'s `#SBATCH` via the Phase-A splice helpers, writes it to the host, and Phase-A `submitSlurmJob`) → **Watch** (the log tab is already tailing; points at the Jobs view). The **Workspace** step is why project creation moved to the end of it: a cluster's `$HOME` is a small quota'd code filesystem, so the data belongs in a time-limited workspace on the parallel filesystem — and the integration is exactly one assignment, `remoteChosenPath = <workspace path>`, since `buildRemoteSpec` derives the host root from it and **every** transport (SFTP upload, byte-sync, git lockstep, run tabs) follows. The alternative layout (project in the browsed `$HOME` folder + the workspace symlinked in as `./data` via `hpc_ws_link`) is offered with its caveat stated: byte-sync never follows a symlink. A host with no workspace tooling skips the step's content and keeps the same single Create button. The step also offers the **home anchor** (default on): a small `~/eldrun/<project>/` with `logs/`, a `workspace` symlink and an append-only record — the only thing that still names the workspace after expiry deletes it — after which the starter script's `--output` is spliced to write job logs there. `HpcPipelineWizardHost` is mounted once in `AppShell`, driven by `stores/hpcPipeline`. Untested. |
| `projects/RemoteMachinesWindow.tsx` | **The one unified remote hub** (`docs/multi_host_remote_plan.md` §4.4): one entry per host — the **primary** first (it owns files/git/mirror; Connect/Manage only, no sync/pull/remove), then each extra "worker" machine — plus **Add a machine**. Every host's Connect/Manage opens the shared per-host `RemoteConnectDialog` (primary → `"primary"`, worker → its id), which renders on an *elevated* backdrop (`.modal-backdrop-elevated`) so the detail sits above this hub. Workers also get "Sync code now", "Pull outputs…" (size-confirmed), and the shared-fs/`sync_code` toggles. The Add-machine form opens on **"Your machines"** — the header's **global machines** (`stores/globalMachines`), offered here rather than only from the other end: a global machine is already set up and already authenticated, so retyping its address and logging into it a second time was work the dialog could do itself. A row's **Add** seeds the same confirm-shared-path panel the header menu's handoff does (`pendingDrop` → add + connect, shared-fs), so there is one add-and-connect path, not a second copy of it; **Use in the form** just fills the address/user/label for the ordinary flow (browse the folder, or sync a copy). A machine already this project's primary or worker — matched by SSH **target**, never by id, since a project host is a copy by value (`lib/machineSync`'s `sameTarget`) — is shown as "Already added" with no action. The Add-machine form's remote path can be **typed or browsed** — a **connect-then-browse** picker that runs on the SHARED login mechanism (`useRemoteBrowse` hook + `RemoteFolderBrowser` component, the exact same connect/list/mkdir machinery the new/extend-project flow uses), so there is one implementation of "log in to a host and browse it", not a worker-specific copy. Only the fields around it are this dialog's own. It connects with a transient, unsaved password (key-auth if blank) — **or**, with **"Sign in in a terminal"** (`TerminalSignInToggle`, on by default in non-headless mode), in an embedded login terminal whose ControlMaster the browse then rides via `useRemoteBrowse.openSession`, so a host that asks for a challenge code can be added at all — and browses to the folder: the existing shared folder for a shared-fs worker, or the (creatable) destination for a synced copy. Standard `modal-backdrop`+`project-dialog` chrome (worker cards = `.remote-machine-*` in themes.css). Mounted once as `RemoteMachinesDialogHost` in AppShell, driven by `stores/remoteMachines`; opened from **any of the pill's connection lamps** (`header/RemoteConnMenu` — every lamp now opens this hub, not a per-host connect), the pill's Runtime item, and the file viewer's **SSH (remote) type tag** right-click (`ProjectFilesView`, now a single "Remote machines…" item). Workers are one-way read-only experiment machines; the primary stays the project's `remote`. |
| `projects/useRemoteBrowse.ts` | **The** shared "log in to a host + browse its filesystem over SFTP" state machine — the ONE implementation of *how* a remote connect + folder browse works, so the primary machine and further (worker) machines never carry two copies. A live session is a FROZEN `(conn, password)` pair (a credential edit means `reset`+reconnect); the listing auto-refreshes on path/conn change. `connect` (ssh_connect + remember + open start dir), `openSession` (freeze a session another path already authenticated, e.g. a non-headless login terminal's ControlMaster), `enter`/`goUp`/`jump`/`mkdir`/`reset`. Used by `useRemoteSession` (new/extend-project) and `RemoteMachinesWindow` (add-worker); each keeps its own fields/steps/VPN around it. |
| `projects/RemoteFolderBrowser.tsx` | **The** remote folder-picker UI (`.remote-browser` chrome: breadcrumb, go-up, recently-used jump, inline new-folder, listing, footer), rendered by both the new/extend-project flow (`RemoteProjectSection`) and the add-worker flow (`RemoteMachinesWindow`) so the browse step is one component, not two. Owns only the new-folder input text; everything else is the caller's (fed from `useRemoteBrowse`). Exports `remoteEntryExt` (file-type icon helper). |
| `projects/TerminalSignInToggle.tsx` | **The** "Sign in in a terminal" switch — the non-headless login offered **per connect**, on both channels of both connect dialogs (`RemoteConnectDialog`, `RemoteProjectSection`), default **off**. It exists because headless can only ask what it has fields for: a host or tunnel that answers with a challenge/OTP or a second prompt can never be logged into from them, however many retries. Flipping it on swaps the password fields for the same embedded login terminal `connections_headless: false` uses. Never writes the global setting (a mode is the user's statement, not a failed handshake's), never offered on Windows (no ControlMaster to ride — the `winManual` reason), and never discards a saved credential: the Save password row renders from *both* halves of the section. The VPN modal's equivalent is a button (`stores/vpnPrompt`'s `handoffToTerminal`). See `docs/context/remote_credentials.md`. |
| `projects/CredentialPasteBar.tsx` | **The** "Type it for me" row above an embedded **login terminal** — the buttons that put a known login name, and a *saved* password, at the terminal's cursor. It exists because a terminal login and a saved credential were never the contradiction they look like: non-headless means Eldrun does not *handle* the secret (the host asks, the user answers, in a terminal they can see), not that the keychain is empty — a password saved from a headless connect, or from the header's VPN menu, was simply unreachable there and got retyped into every login. The secret never enters the frontend: a password button calls the backend `credential_paste_to_pty`, which reads the keychain and writes the bytes into the PTY itself; only non-secrets (an SSH login name, a VPN auth username the dialog already shows in a plain field) are pasted from here via the ordinary `pty_write`. Nothing is submitted — a paste is not a login, so the line stays correctable until the user presses Enter. `sshPasteEntries`/`vpnPasteEntries` build the button sets once for all three surfaces (`RemoteConnectDialog`, `RemoteProjectSection`, `RemoteMachinesWindow`); an unsaved target (or a locked keyring, which reads the same) reports "nothing saved for this login" rather than pasting an empty secret. Untested. |
| `projects/BigFolderExcludeDialog.tsx` | "These folders are giant — sync them?", asked **once**, at setup (opened by the new/import-remote and extend-to-remote flows via `stores/bigFolders`, and re-openable from the file view's "Large folders…"). Mounted once as `BigFolderDialogHost` in AppShell, for the same reason the remote-machines manager is: the project that asked may not be active by the time its census returns. Lists each folder with **both** sides' numbers (a dash where the folder doesn't exist there — at setup that distinction is the whole point: host bytes are usually experiment output that should stay, local bytes are usually a build/venv that should never go up), ticked to exclude by default, and re-runs the census when the project's SSH lamp turns green so a not-yet-connected project fills in its host column. Writes `sync_set_excluded` — the marker the whole-project pull/push honours, not just the auto engine. |
| `projects/BoxPill.tsx` | Project-box pill (meta-grouping, #13/#41): hover dropdown of members, drop target for additive adds, menu into the box editor. Dimmed when empty — an empty box survives (dissolve is the editor's explicit action). |
| `projects/BoxEditorDialog.tsx` | The box editor (rename / full member checkbox list / confirmed Dissolve / container-VM trust notice), mounted once in `AppShell` (`BoxEditorHost`), driven by `stores/boxEditor`. |
| `projects/ActivityCalendar.tsx` | Per-project activity calendar heatmap (unrelated to the calendar tab). |
| `projects/scaffold.ts` | Pure helpers: name sanitize, SSH-address parse, scaffold/description fill prompts. |
| `calendar/CalendarPane.tsx` | Calendar tab shell: toolbar, search, ICS import/export, view switch. |
| `calendar/TimeGrid.tsx` | Day/week hour grid: positioned blocks, drag-create/move/resize, now-line. |
| `calendar/MonthView.tsx` | Month + multiweek grid, with multi-day spanning bars. |
| `calendar/AgendaView.tsx` / `calendar/TasksView.tsx` | Flat upcoming list; to-do (VTODO) list. |
| `calendar/EventDialog.tsx` | Event editor (repeat, reminders, category) + this-vs-all-occurrences prompt. Also the **video call** field (`lib/conference.ts`): a URL-typed input beside a Join button, plus a "detected in this event" hint when the link had to be *derived* from the location or the notes. The derivation is deliberately never written into the field — freezing a guess into the user's own data would leave no way to tell the two apart afterwards, and no way to correct it except by deleting something they never typed. The Join is enabled only for a link `isJoinableUrl` accepts, since a half-typed URL is this field's normal state while it is being filled in. |
| `calendar/CalendarSidebar.tsx` | Mini-month + calendar list (color, visibility). A CalDAV-backed calendar also carries its own sync affordance (`⇅`, `…` while running, an amber `!` when the last sync failed, with the backend's own sentence as the tooltip) — separate from the ICS feed's `↻` because the two are the same *kind* of action but not the same path, and because a failed **unattended** sync has to be visible on the row: the dialog that would otherwise carry the message is exactly the surface nobody has open when the timer fires. |
| `calendar/CalDavAccountDialog.tsx` | The CalDAV account editor (`docs/caldav_plan.md`) — `MailAccountDialog`'s structural twin, deliberately: same three-part problem (a user-typed server, an opt-in password, a background interval), so the same three-part answer rather than a second hand-rolled one, down to `SavePasswordRow` off `useSavedCredentialSource` and `true \| null` never bare `false`. What it adds is the step CalDAV needs and IMAP does not — **discovery**: a server has a home set of N collections, some shared with you by someone else, so the flow is find-then-pick and each tick becomes a sidebar calendar. There are **no presets and never will be**: a base URL is a login on someone's infrastructure, and defaulting it from an email address would sync the wrong account (this repo is public, so a preset list could only name institutions anyway). App-specific passwords are mentioned in the hint rather than assumed. |
| `calendar/CalDavSyncHost.tsx` | The scheduled half of CalDAV sync — `MailIndicator`'s check-interval timer, for calendars. Renders nothing, mounted once at the shell rather than in the calendar pane: the surfaces that read a synced calendar (the header badge, the board's agenda rail, the reminders) are not that pane, so refreshing only while it is open would leave the calendar stale exactly where it is looked at. Mail's three rules apply verbatim — it costs nothing when no account exists (the accounts read is one local file), the first tick is a whole interval away (checking at mount is checking at launch), and an explicit `0` means never. The backend's ctag check is what keeps a short interval cheap. It is also where the **push** half is installed (`installCalDavPush`): exactly one handler may answer for a row's server side, so registering it at module scope in a file two React roots import would install two — and an edit made from the to-do board or the header's day list has to push the same way one made in the calendar tab does, none of which is the calendar pane. |
| `calendar/CalDavConflictDialog.tsx` | **The** answer to a CalDAV `412` (Phase 3), mounted once at the shell beside `CalDavSyncHost` and for its reason: the edit that conflicts can come from the calendar tab, the overlay, the board or the header's day list, and the surface that would carry the message is whichever of those has already been closed. A conflict means both versions are real edits, so the app must not pick — the three answers are **keep mine** (re-read the server's current ETag and write against *that*, so it is an overwrite but still a conditional one), **use the server's** (a forced sync of the collection, through the same merge every other sync goes through) and **decide later**. There is deliberately no *merge*: half of one time and half of another is not a third valid meeting. One question at a time, oldest first — a stack of modals is unanswerable, and taking the server's copy re-syncs the whole collection, which can resolve a sibling conflict on its own. Untested. |
| `calendar/IcsImportReviewDialog.tsx` | What is in a picked `.ics`, shown before any of it is imported (`lib/icsSafety.ts`). Explicitly **not** a quarantine gate, and the footnote says so — a calendar file cannot run anything here. It answers the question the defences cannot *because* their job is to be silent: a `PROCEDURE` alarm, an `ATTACH`, a `zoommtg:` location or a right-to-left override in a title are all dropped or cleaned today without a word. Every row says what Eldrun **does** about the finding, since a warning that does not say "ignored" reads as a threat rather than a fact; and the dialog is not raised at all for a file with no findings, which is what keeps it worth reading on the file that has some. Untested. |
| `calendar/CalendarOverlay.tsx` | **Calendar as a global app** (`calendar_global_app`, off by default, no experimental gate above it — the calendar is shipped and reads one local file): the header 🗓 button's overlay, mounted once in `AppShell` as `CalendarOverlayHost`. The twin of `mail/MailOverlay.tsx` and the same bargain — it renders the *same* `CalendarPane` a tab renders, so the overlay and any open tab are two views of one `calendar.json` and cannot drift; it adds only a size and a way to close it, on the shared `.project-dialog.dialog-framed` dialog chrome. |
| `calendar/AlarmPopup.tsx` | In-app reminder popup (snooze/dismiss); mounted in `AppShell`. |
| `todo/TodoOverlay.tsx` | **The global to-do board** (`todo_board`, off by default, no experimental gate — its cards *are* `calendar.json`'s tasks, so it reads one shipped local file): the header ☑ button's overlay, mounted once in `AppShell` as `TodoOverlayHost`, **last of the three** overlay hosts because all three are `.modal-backdrop` at one z-index and DOM order is the tie-break. There is deliberately no todo *tab* — the store is the calendar's, global across every scope, so a scoped tab could only show the same board while being left behind by a project switch (the redundancy that retired the mail tab). Not mounted in a popout: no header to open it from, and `stores/todo` is per-window. |
| `todo/TodoPane.tsx` | Filter bar + board + the two rails. Renders `applyPending(tasks, pendingOrder)` — the optimistic overlay a drag stages — never the raw store, which is what keeps a dropped card from visibly snapping back while the write is in flight. |
| `todo/TodoBoard.tsx` | The Trello-shaped board **and the owner of the card drag**. Three rules, all forced by the engines: the commit is bound through `bindDragRelease` **synchronously inside pointerdown** (WebKitGTK never delivers the terminal pointerup to listeners added mid-gesture — `TabBar` documents the same trap), pointer capture goes on `documentElement` and never on the card (which unmounts mid-gesture as the placeholder re-renders), and because `pointercancel` **commits** on Linux the commit must be safe under a target the user never chose — no column under the pointer means the card simply stays put, never a default column and never a delete. A drag is ONE backend write (`todo_move_tasks`), whatever it moved. A fourth rule is the *board's* rather than an engine's: **one index space, from the hit-test to the wire**. Every index in the gesture is counted with the dragged card taken out of its column — that is what `insertionIndex` measures (it is fed the other cards' rects), what the rank bisect uses, and what `TaskPlacement.index` means in Rust — so the preview has to be drawn in that space too. It was not: the source column left the card mounted and faded, which shifted every slot below it by one and made the placeholder offer the card's *own* position as the new one. `TodoColumn` now renders it out of the list, and the drag **opens** on the slot it was lifted from (`CardDrag.fromIndex`, from `currentSlot`), so the placeholder replaces the card in the same frame and nothing changes height at pickup. That same slot is what makes a drop back onto it recognizable as the no-op it is and skipped rather than written — for a card that already *has* a placement; an unranked one (everything the Tasks view and ICS import create) is only being *displayed* there, and the drop is what gives it a real slot. |
| `todo/TodoColumn.tsx` / `todo/TodoCard.tsx` | A column (header, cards, add-a-card) — which during a drag renders the dragged card **out** of its list rather than leaving it faded in place, because the placeholder's index is the board's exclusive-of-that-card one (see `TodoBoard.tsx`) and the two lists have to be the same list — and a card (checkbox, inline-editable title, due/priority/tag chips, checklist, project chip, mail link). **Adding a card opens the full card dialog** — the same editor an existing card is edited in, not a title-only inline composer. The composer wrote the row to `calendar.json` before anything else about the card was known, so backing out was a *delete* of something that already existed and every card still needed a second pass through the dialog for its due date; the dialog stages the whole card and writes on Save, with Enter on its title field keeping the one-keystroke add for the card that needs nothing else. The column therefore hands up a **draft** (`TodoBoard.addCard` builds it — placement, calendar, the filtered project — and never writes), and `task.id === ""` is the dialog's only signal that it is creating rather than editing: read off the record, not passed as a flag, so the two cannot disagree. Every inner control stops the pointer, or clicking a checkbox seeds a drag; every text input stops its own Escape, or renaming a card closes the whole overlay (`stopPropagation` does not stop sibling listeners on `window`). The card's **checklist is inline, shown by default and editable on the board** — a step is ticked, added, reordered or dropped without opening the dialog, because breaking a card into steps is something you do *while* looking at the column you were reasoning about. Open by default because the bar and the list say different things: "2/5" is a number, *which* two is the reason to look at the board — and while the bar was the only disclosure control, a card whose steps were all done showed a full bar and nothing else. The bar is now the **fold** control (with a caret, so a folded card does not read as one whose steps failed to load) and the head's `＋` opens the **composer**, which is the only part that is transient — a permanent input under every card is what open-by-default would otherwise cost. A fold is remembered per card in `stores/todo`, not in the component, which unmounts with the overlay. It writes on **every edit** (a board has no Save, so a staged checklist would be a draft the user cannot see is unsaved) and reads the card back out of the store rather than off its props first, or a second Enter one frame after the first would drop the step the first one added. Renaming a step is still the dialog's — there is no room for a text field per row here. **Steps also reorder by dragging a row's grip** (`todo/useStepReorder.ts`, shared with the dialog): a checklist is an ordered list the moment it has three entries, and until that gesture existed the only way to move a step up was to delete the rows under it and retype them. The ops themselves are `lib/todoBoard`'s, shared with the dialog. |
| `todo/TodoCardDialog.tsx` | The full card editor on an *elevated* backdrop — and, since the column's composer went, the surface a card is **added** in too. One dialog for both because a new card wants exactly the fields an existing one does; an empty `task.id` is the whole distinction, and it decides three things: `createTask` instead of `updateTask`, no Delete (Cancel already discards a row that was never written), and a Save that is disabled until the card has a title. The checklist deliberately does **not** drive `percent`: deriving it would make the progress control unusable and — since 100% moves a card to Done — ticking the last checkbox would silently relocate the card. Clearing the linked mail's Urgent mark is an explicit checkbox, never implicit, because mail may be switched off. Its checklist reorders by grip exactly as the board card's does (`todo/useStepReorder.ts`) — the difference is the dialog's throughout: the move is staged in the draft, the card writes it. Also the **linked appointment** block, `linked mail`'s twin: what the card was converted from, an unlink, and (only where the calendar overlay can actually open) a way into the calendar. The **due time** is two `<select>`s (`00`–`23`, `00`–`59`) beside the date rather than any kind of typed clock: `common/TimeField`'s segments are a 2ch target that reads as broken chrome here and shows nothing landing as you type, and the native `<input type="time">` takes its AM/PM face from the engine locale in defiance of `Settings.time_format_24h`. A list of hours is 24-hour by construction and has nothing to mistype. Clearing the hour (`--`) takes the card back to a whole-day deadline — the only way out — and the minute list waits for an hour, as both wait for a date. |
| `todo/useStepReorder.ts` | **The** checklist-step drag, shared by the board card and the card dialog. Pointer events, not HTML5 DnD (WebKitGTK, and the dialog's list sits inside a modal where a missed drop never fires and would strand a row); the **grip** takes the pointer capture, so the terminal event arrives at an element that is still mounted while the rows around it re-render, and so the row's checkbox/title/delete stay clickable and the board card's own drag is never seeded; the DOM order is **frozen** for the gesture (rows measured once, the others part by `transform` only) so the rects stay true. A `pointercancel` here does *not* commit — unlike the card drag, where cancel is the terminal event of an ordinary drop. ↑/↓ on the focused grip is the keyboard equivalent. |
| `todo/TodoMailRail.tsx` / `todo/TodoAgendaRail.tsx` | Urgent mail (from the existing Important/Urgent marks) and today's + tomorrow's appointments. The mail rail calls `mailPriorityPage` **directly**, never `useMailStore.openPriority` — that action replaces `headers`/`selectedPriority`, i.e. it would retarget the mail overlay's list under the user once a minute; its 60 s poll is defensible only because that command is a local SQLite read, and the `mail_client` gate is checked **before the invoke** because opening the mail store *creates* the mail database. The agenda dims past occurrences rather than hiding them, so the rail cannot disagree with the header's calendar badge. **Both rails convert into a card**, through the *same* builder (`lib/todoBoard`'s `taskFromMail`/`taskFromOccurrence`) and into the same column — the board's first — so a board never holds two kinds of converted card. What differs afterwards is deliberate: a converted mail *leaves* the mail rail (that list is things demanding an answer, and the row disappearing is the feedback), while a carded appointment **stays** and merely stops offering a second card — this rail is *the day*, and a 10:00 meeting that vanished because it had a card would put the rail at odds with the calendar and the header badge. |
| `mail/MailPane.tsx` | The embedded mail client (behind the `mail_client` experimental flag): folder rail / header list / message view. **The rail is two labelled zones with a divider, not one column of headings** — account-*independent* above (Important/Urgent, and the keyword rules that fill them, on a separated row inside that group since it configures the two lists rather than being a third one), account-*dependent* below (the accounts, then the selected one's folders, whose heading names that account). Read top-down as a single hierarchy, two cross-account lists sitting above the account list still looked like they might belong to the first account, and "your keyword rules" sat beside "this account's Sent folder" as though they were the same kind of thing. The toolbar carries the same split as a hairline: account/folder actions left, mailbox-wide ones right. Global store, no PTY, no project props — the same shape as `CalendarPane`. **It has exactly one host, `MailOverlay`** — there is no mail *tab* any more. There was one, and it was redundant from the start: the store is global, so the tab showed the same mailbox from every scope while still belonging to a project you then switch away from. The kind is retired via `stores/tabs`' `RETIRED_TAB_CMDS`, which drops a mail tab out of a layout persisted before the removal **unconditionally** — unlike `withdrawnTabKinds` (a flag that might come back on, so it waits for settings), a retired kind can't return, and `cmdToKind`'s fall-through for its unrecognized `cmd` is `"shell"`, i.e. a terminal that would try to run `__eldrun_mail__`. **It never connects on its own**: mounting reads the local index only, and the single path to a socket is the *Check mail* button. Arrival *announcements* are `MailIndicator`'s — the header badge is mounted whether or not this pane is, so the pane's old note strip was a second copy of it. The rail's top section is **Priority** — the **Important** and **Urgent** lists, above and outside Accounts because that is what they are: every account's marked mail in one place, and nesting them under an account would say the opposite. Both render even when empty, since an empty list is where the feature is discovered. Opening one **unlights the account chip**: the account selection survives (the folder rail below still needs an account, and leaving the list has to put you back where you were), but the chip is a claim about what you are *reading* and a cross-account list is not that account's mail — the rule `MailIndicator`'s dropdown already draws its account rows by, applied here rather than restated. And the toolbar's "Mark all as read" gains a counterpart in the same slot, **"Empty this list (N)"** (`stores/mail`'s `clearPriority` → `mail_priority_clear`), shown only while a priority list with something in it is open: marks accumulate and are never consumed, so without it the way back to an empty Urgent list is one right-click per message. Confirmed before it runs — nothing moves and nothing is deleted (a mark is not a folder), but the filing is the user's own work and there is no undo — and deliberately *not* optimistic in the store, since a page blanked before the write landed is indistinguishable from a write that failed and took the mail with it. |
| `mail/MailOverlay.tsx` | **The** mail surface (`mail_client`): the header ✉ button's overlay, mounted once in `AppShell` as `MailOverlayHost`. It renders `MailPane` floated over the window instead of tiled into a scope — which, since the mail tab was retired, is the only way mail is on screen at all; all this adds is a size and an Escape/backdrop close. **One gate, not two**: `mail_global_app` used to sit under `mail_client` to decide whether the header carried a button *as well as* the tab, and with the tab gone that second switch could only ever mean "mail on, and unreachable" — so it is gone from `Settings` on both sides (an old `settings.json` still holding the key is fine; serde ignores fields the struct no longer declares). It rides the app's canonical dialog chrome (`.project-dialog.dialog-framed` + `.settings-title-row` + `.dialog-close-btn`), applied exactly as `GlobalMachineMonitorDialog` applies it to the system-monitor pane — same shape of thing, a whole pane in a dialog, sized by its own rule with a body that clips rather than scrolls. At the shell rather than in the header because it covers the window and must survive a project switch. |
| `mail/MailList.tsx` | The header list. Enforces two threat-model rules in the rendering itself: the **addr-spec is always shown** beside a sender's display name (an attacker-chosen name must never stand in for identity), and every mail-derived string is a plain text node. The list is a real **column grid** — star · read-dot · clip · sender · size · date — whose template (`--mail-cols`) is declared once on `.mail-list` and used by both the header row and every message row; that shared declaration is what makes them line up, and every column but the sender is a **fixed** width because each row is its own grid container, so `auto` tracks would size per row and turn the columns into a staircase. The clip is a column with an empty cell rather than a marker in front of the subject (a marker that shifts the subject when absent is not a column), and the size is printed on every row rather than only under its own sort, so selecting that sort does not move the other columns sideways. **The sort controls are the column headers** (star / clip / size / date — click to sort, click the sorted one to reverse, arrow only on the sorted column), which is the one thing the list does *not* implement: it is handed one page, so it hands the pair back through `onSort` and the ordering happens in SQLite over the whole folder — the biggest message in a mailbox is rarely among the newest hundred. The **sender** header is deliberately inert: `from` is stored as JSON, so ordering by it would sort by `{"name":…`, and a header that sorts wrongly is worse than one that does not sort. Also the **right-click menu** that files a message into **Important** or **Urgent** — the only way in, because two marks plus an unmark is three glyphs the rows nobody is filing would pay for. Portaled to `<body>` at the cursor (the pattern `ProjectFilesView`'s type-tag menu uses): this list scrolls and clips, so an in-flow menu on the last visible row would open inside the overflow. Right-clicking also *selects* the row — the menu naming the wrong mail while the message pane shows another is how the wrong thing gets filed — and the menu quotes the subject (stripped of format controls, like every other place a subject is printed) plus a footnote saying what "move" does **not** do: nothing leaves the folder it is in. A row carries its mark as `!`/`!!` wherever it appears, not just in the priority list, because that is the only place the user can see the filing worked. |
| `mail/MailMessageView.tsx` | The message pane. The body renders in `<iframe sandbox="">` with **neither `allow-scripts` nor `allow-same-origin`** (together they are a total sandbox escape) plus its own `<meta>` CSP — an inline meta rather than the `csp=` attribute, which WebKitGTK does not implement. The sanitized HTML carries **no `href`**: links are `data-lid` markers resolved against `MailBody.links` and opened only after a confirm that names the real host. Remote content is blocked with **no control offered to unblock it** — the backend has no image proxy yet, so a "Load remote content" button would clear the banner, report success and fetch nothing; blocked and honest beats a control that lies. Attachments Save via `mail_attachment_save` (the backend raises the OS dialog) or preview in-pane; there is deliberately no "open with the system app". Also hosts `MailCryptoPanel` — end-to-end signatures/encryption, **beside** the sender checks rather than merged with them, because they answer different questions (what your server concluded about the delivering hop, vs. what the sender did before the message left their machine) and a message can pass one while failing the other. The chrome rule is the backend's: the component renders `info.state` and `mailCryptoTone` is the single mapping to appearance, so `known` — a valid signature from a key nobody checked — is *neutral*, never green. Also hosts `MailAuthPanel` — the **sender checks** (SPF/DKIM/DMARC) read out of `Authentication-Results`, shown only when the account named a trusted `authserv-id` *and* the topmost header carried it; a configured account that got someone else's header gets a named warning, and a message with no header at all renders nothing, because absence is not failure. |
| `mail/MailEncryptionDialog.tsx` | **The local store's encryption** (`docs/context/mail_encryption.md`), one dialog with three faces chosen by `MailEncryptionState` rather than by a prop, so it cannot disagree with the backend about what is going on: **unlock** (a passphrase store currently showing its memory-only stand-in), **offer** (never asked — and for an install that already holds mail, "Delete and download again" is offered *beside* migration rather than buried, because a migration cannot reliably erase what it replaces on an SSD), and **status**. What the copy must not overstate is the whole design: FDE already covers the stolen laptop, so what this adds is backups, copies, cloud-synced folders and multi-user machines — and nothing at all against a live process. A security feature that oversells itself buys behaviour changes it did not earn. |
| `mail/MailKeysDialog.tsx` | **The OpenPGP keyring.** Built around the fingerprint, because with no certificate authority the fingerprint *is* the trust model: shown in full, grouped in fours, monospace, wrapping, **never truncated** — 40 run-together hex characters do not get compared, they get glanced at, and an abbreviated one is worse than none because it still looks checked. The "I checked this with its owner" box is the only path to positive chrome anywhere in the feature, and it is reversible, because a claim the user made can turn out to be wrong. Export is public-only and says so: "export my key", typed by somebody who means the public half, is the standard way a private key ends up in an email. |
| `mail/MailAccountDialog.tsx` | Account editor. Save-password is opt-in, **default OFF**, and sends `true | null` — never `false`, which would *clear* the credential. Renders the shared `SavePasswordRow` off `useSavedCredentialSource`, so the tri-state, the 4 s keychain bound and the locked-keyring banner are the SSH dialogs' implementation, not a second copy. Presets are generic public providers only; an OAuth-only one is offered disabled with the reason. |
| `mail/MailFiltersDialog.tsx` | **The keyword filters** — words that file arriving mail into Important/Urgent, and the manual half of the feature on purpose (the words are the user's own, so *why* a message was filed is answerable by reading the rule; a local-model classifier is a later, separate thing that must not be able to pass for this one). Four things about it are decisions. **It states what a rule cannot do on its face**, not in a tooltip: the mark is local, rules run on mail that *arrives* plus an explicit re-run, they search the stored **snippet** rather than the body, and Sent/Drafts/Trash/Junk are out of scope — a filter dialog is where people form their model of what the software is doing to their mail, and every wrong model here is expensive. **The preview is a dry run of the real thing** (`mailFiltersApply({dryRun:true})`), so the number comes from the code that would do the marking; a TypeScript copy of the matcher would be a second implementation of the one thing this feature must never be ambiguous about. It tests the rule **as edited**, switched off or unsaved, because that is when "what would this catch" is asked. **Order is data** — first match wins — so the list reorders and saves wholesale. And **a rule that would never fire is an error**, not a warning: no terms or no fields saves happily and then does nothing, which from the user's side is indistinguishable from a broken feature. `src/lib/mailFilters.ts` is its pure half (term parsing — commas and newlines split, a space does *not*, so `board meeting` stays one phrase; case-insensitive dedupe; field toggling that preserves display order; the problem tokens). Untested. |
| `mail/MailComposeDialog.tsx` | The composer. Sign/Encrypt are per-message, **both default off and never remembered** (a sticky "encrypt" that silently turned itself off once would be worse than one always chosen), offered only where `mail_pgp_available` says they can be honoured, and the missing-key check runs *while the message is being written* rather than on Send — the send would refuse anyway, since the backend never downgrades to plaintext, but learning it after writing the message is a worse way to find out. Attaching is a **backend** action (`mail_attach_pick` raises the OS picker inside Rust and copies the files into the mail sandbox dir), so this component never sees or constructs a filesystem path — the draft is saved first only because the pick is keyed by draft id. |
| `browser/BrowserPane.tsx` | The in-app browser tab (behind the `web_browser` experimental flag). **An ordinary DOM pane, not a native view**: Plan C proved an in-pane child webview is impossible under WebKitGTK (`set_bounds` is a no-op for a GtkBox-packed child), so the tab renders **reader mode** — HTML the backend fetched and sanitized with the mail client's `ammonia` pipeline — and a real engine is a *separate hardened window* one explicit click away. Nothing about mounting reaches the network: a restored tab shows its resume card holding the persisted URL, and only `browser_restore_navigate` (default OFF) changes that. The live-page control is **hidden, not disabled**, where `browser_capabilities()` says live windows are unavailable — the capability is asked of the backend at runtime, never inferred from a platform check here. Two independent reasons put it there and the frontend deliberately cannot tell them apart: the platform (Windows in v1, whose *reader* mode works fine) and `browser_live_pages`, the **default-off, off-in-debug-too** opt-in a live page needs because a page that runs real JavaScript can reach loopback services by name and over `ws://`, neither of which is fixable from app code. Retitling from the page title is main-window-only and guarded by `autoLabelRef`, the `fileTabSync` rule: refresh the label only while it still equals the last auto-derived one. |
| `browser/BrowserReaderView.tsx` | The fetched page's body — `MailMessageView`'s containment applied to a web page, for the same reason and with the same rules: `<iframe sandbox="">` with **no tokens** (no `allow-scripts`, no `allow-same-origin`), its own inline `<meta>` CSP (the mail constant *imported*, not copied — `csp=` is a Chromium feature WebKitGTK does not implement), the srcdoc assigned through the DOM property, and `readerLooksUnsafe` (mail's tripwire) refusing to render at all if live markup survived the backend's sanitizer. No `dangerouslySetInnerHTML` anywhere in this directory, asserted by `BrowserTripwire.test.ts`. |
| `browser/BrowserAddressBar.tsx` | The address field as a **security control**: origin emphasis (the host at full weight, everything else muted), the host **never** ellipsis-truncated (a shortened host is the attack), userinfo flagged with the host taken from the *parser* not a string search, and a punycode host showing the ASCII form beside the Unicode one. `Enter` commits through `lib/linkTarget`'s `parseAddressInput`, which refuses a non-`http(s)` scheme with a reason — a first, independent gate, not the boundary. |
| `browser/BrowserSecurityChip.tsx` | The chip + its portaled popover. Renders `SecurityState` **verbatim** and computes nothing; a `tls` value it does not recognize degrades to unknown, never to secure. The state is a *word*, not only an icon. |
| `browser/BrowserDownloadDialog.tsx` | The download consent dialog. The bytes are already quarantined in a directory the *backend* chose; Save makes the backend raise the OS save dialog, and the single path in the system is the one the user picks there. The frontend names no destination, there is no "save all", and there is deliberately no "open when done". |
| `browser/BrowserBlockedNotice.tsx` / `browser/BrowserStartPage.tsx` | A refused navigation as a *page state* (full URL, monospace, wrapping, never truncated, **no override**) — and, with an `onProceed`, the gate's **third** outcome: a loopback/private address is *reachable* but is asked about first, with an "Open anyway, once" button that is a real user gesture and is scoped to one tab for one session. The reason is always a sentence, never the backend's token (`reasonPhrase`). Plus the blank-tab start page / resume card. |
| `browser/useBrowserEvents.ts` | The backend's browser events, installed **once per window** behind a refcount + generation counter — the flat pane layer keeps every tab mounted, so a per-pane `listen` would raise one download-consent dialog per browser tab in the app, and an async `listen` resolving after a fast unmount/remount would leave two live sets. |
| `browser/BrowserDownloadHost.tsx` | The one mount of the download-consent dialog per window (`AppShell` + `DetachedApp`). It is not the pane's, because the dialog is **portaled to `<body>`** and `CenterPanel` hides inactive panes with `display: none` — a portal escapes that, so a pane-rendered dialog would appear once per browser tab. It also owns the event listeners, so a download raised by a live-page window is answerable when no browser tab is open. |
| `skills/SkillsLibraryTab.tsx` / `skills/SkillsLibraryView.tsx` / `skills/SkillsOverlay.tsx` | **The Skills Library** (`docs/skills_plan.md`) — browse a git-hosted collection of Claude Code skills (plain `<name>/SKILL.md` folders), preview one, and copy it into a project's `.claude/skills/` or into the machine's personal `~/.claude/skills/`. `SkillsLibraryView` owns all of it: the sources bar, the installed list, the filterable catalog and the preview panel. **Install is the preview panel's alone** — never a one-click action off a catalog row — because showing a skill's content before it is copied into agent-trusted territory is the one piece of the feature's original security posture worth keeping even at this scope, and it costs nothing extra to build beyond where the button sits. **The catalog is machine state; only the install is scoped**, which is why one component serves two hosts (`ProjectFilesView`'s bargain): `SkillsLibraryTab` is `ProjectFilesTab`'s thin-host shape, resolving its own project from `scope` so it works the same in a popout, and `SkillsOverlay` is the header 🧠 menu's door, rendering the same view with `projectDir: null` on `MailOverlay`'s chrome and driven by `stores/skills`. The scope is the **view's own state**, not a prop — a host knows whether a project exists, not which scope the user wants next — and it defaults to the narrower one available, since a personal install is read by every project on the machine and should be asked for rather than fallen into. While a project is the target the view *also* reads the personal list (`inherited`), or the catalog's "Installed" badge would report a skill the project can already use as absent; those rows carry their own uninstall and their own confirm, because removing one removes it everywhere. The tab is **not** retired by the overlay the way mail's was — the scope here is real, and the tab is the one surface that knows which project is meant. It is offered at the root scope now (the personal scope gave it something to do there); singleton per scope via `ensureTab`, the calendar/printing bargain. Untested. |
| `printing/PrintManagerPane.tsx` | **The native print manager tab** — the machine's printers, what is queued on each, and the four verbs a queue is worth opening for (make default, pause/resume, test page, cancel). It is what the `print_manager` **global app** used to launch an external GUI for; that role now sits in `RETIRED_GLOBAL_APP_ROLES` beside mail, the calendar and the file manager, for the same reason all three were retired — what was behind the button is a list, and Eldrun can render a list, in this window, under this theme. Takes **no project props** (printers belong to the machine, not a project — the `CalendarPane` shape), which is also why the tab is a singleton per scope. `visible` is not cosmetic: it arms the 6 s queue poll, so a background print tab shells out to nothing and a restored one costs a single `lpstat`. Two rendering rules are the pane's own: **stopped and "not accepting" are shown as two facts**, because a queue that takes jobs it will never print is exactly what one status word hides; and a failed action shows the print system's **own words** (CUPS answers "Forbidden" outside `lpadmin`, and that sentence is the only thing the user can act on). Untested. |
| `header/Clock.tsx` | Header clock. |
| `header/AppTimerDisplay.tsx` | Active-project time-tracking readout. |
| `header/AppResourceDisplay.tsx` | CPU/RAM/GPU readout. The GPU row is the **device's** memory + utilization (`gpustat`), not Ollama's models — those are one line of its tooltip, and the whole reading only on a machine whose GPU can't be read, where it falls back to the old Ollama-only figure rather than showing a zero. |
| `header/ConnTypeIcon.tsx` | Local/remote (SSH) connection-type icon. |
| `header/MachinesIndicator.tsx` | Machine-wide **global machines** (SSH hosts no project owns): the VPN indicator's pattern applied to SSH — authenticated once, with no project or path attached, then connected/monitored/reordered from the header. **Opens on click, never on hover**, and its open-sweep is rate-limited (60 s per machine, in-flight-guarded) and skips HPC-tagged rows — `ssh_probe` is a full authenticated login on each host, so a hover-opened menu logged Eldrun into the whole fleet as the pointer crossed the header. A row reads **two** maps, not one: `status` (a session this app opened) and `reachable` (the last probe), which is what lets it distinguish connected · stale (session held but no answer) · up · no answer · not checked, instead of painting a probe green. A machine reaches a project **only** through a row's "add to a project" picker (`⇥`), which lists the *open* projects and names the outcome for each: a **remote** project gains it as a `shared_fs` compute host (`stores/remoteMachines`'s `pendingDrop` → `RemoteMachinesWindow`), a **local** one is offered "Extend to remote" with it as the primary (`requestExtend` → the target's `ProjectPill`, which owns that dialog). Attaching is deliberately **not** a drag: a native HTML5 drag out of a hover-menu is unworkable under WebKitGTK (the menu closes or hangs mid-drag, and a drop that misses its target never fires, leaving the row stuck in a dimmed drag state nothing can clear) — and its target lives outside this list anyway. Rows *do* **reorder by dragging their grip**, on **pointer** events (the same choice `TabBar`/`YamlTree` made), with a keyboard ↑/↓ equivalent on the focused grip: the DOM order is frozen for the gesture and the other rows merely `transform` aside to open the landing slot, so the rects measured at pointerdown never shift under the cursor they're compared to. **Removing a machine actively disconnects it first** when it is live (`stores/globalMachines`' `remove` → `disconnect`) — dropping the row otherwise strands an SSH master, and any tmux job under it, with no handle left in the UI. Also the one entry point to **"Remote host usage…"** (`common/RemoteUsageWarningDialog` via `stores/remoteUsage`'s `open`): that report used to put itself on screen after every remote connect; it is on demand only now, and its subject is **this list** — a section per global machine in this menu's order, then the active project's own hosts not already in it (matched by SSH target, never by id) — read on demand rather than swept: opening it used to authenticate against every machine ever added, so a disconnected or HPC-tagged host now renders from cache with its own **Read now**. The **add-a-machine** form also carries **"Sign in in a terminal"** (`projects/TerminalSignInToggle`, on by default in non-headless mode): the login goes to the **root terminal** rather than an embedded PTY (a hangover from when this menu closed on mouse-out; it now opens on click and dismisses on Escape / an outside click) — and once a credential-less `ssh_connect` rides the master it left behind, the machine is `register`ed (persist without a second authentication) rather than `add`ed. The **edit-connection** form carries the same toggle, for the same reason (a re-authenticate that now hits a challenge/OTP can't come from one password field): its poll adopts the terminal session and then persists the edited identity with `update(..., { connect: false })` — never a second `ssh_connect` — lighting the lamp itself via `setStatus` (the write `lib/machineSync` propagates), since a changed target rules out `register`, which is idempotent by target and would mint a new row. Two probe commands sit behind that one list: a global machine authenticates ad-hoc (`global_machine_usage_check`, the twin of `global_machine_monitor_snapshot`), a project host rides its pooled ControlMaster (`remote_usage_check`); same script, same parser, so the sections read alike. |
| `header/MailIndicator.tsx` | The header's ✉ button — mail as a **global app**, and since the mail tab was retired the only way in. Behind ONE switch (`mail_client`, off for a non-debug user); the old `mail_global_app` sub-toggle went with the tab, because a switch hiding the only surface while leaving the feature "on" has nothing left to mean. Its red dot is **unread inbox mail, derived from the local index** (`stores/mail`'s `inboxUnread`), not a counter the component accumulates — the distinction is why it is visible at all. An accumulated "arrivals" count starts at zero on every launch and only moves when a sync happens to run *while the window is open*, so the case a badge exists for (mail that landed overnight) showed nothing; derived, it is right at first paint, survives a relaunch, and falls as messages are read instead of needing dismissal. `refreshUnread` is what puts it there — accounts **and** their folder counts, a purely local read, so a number at launch costs no socket. **Inbox-only**, because a folder a filter already sorted into is not something to be alerted about, and per-folder counts are the rail's job (there you can see *where* the mail is; one header number cannot say). The `mail:new` listener lives **here, not in `MailPane`** — the whole point is to report mail when no mail surface is on screen, which after the tab's removal is the normal case (and a popout, which has no header, therefore never double-counts a delivery) — and it *refreshes the counts* (`noteArrival`) rather than incrementing a rival number, so a sync this window did not start still moves the dot. What survives of the old idea is `newCount`, purely as **emphasis**: a `fresh` ring on the same badge while something has arrived since you last opened mail, never a second figure to reconcile against the first. Reading a message is the ordinary way the number goes *down*, which is why `setFlag` re-reads the folder counts after a `seen` change. It also owns the **only** thing in the mail feature that reaches the network without a click: the `mail_check_interval_min` timer, gated on `mail_client` — which, being off for everyone outside debug mode, is what keeps the mail store's "nothing connects on its own" rule true by default. The interval itself is **no longer** a second opt-in: it used to default to *never*, which made the badge unable to light up at all (it counts arrivals a *sync* found, and the only sync left was a manual *Check mail* — nearly always clicked from the overlay, which acknowledges the arrival in the same gesture), so an unset value now means `DEFAULT_MAIL_CHECK_MIN` (5 min) and an explicit `0` is still honoured. The first tick is a whole interval away on purpose: checking at mount would be checking at launch. The button also carries an amber `!` when the last check **errored** — the pane's error strip is exactly the surface nobody has open when the poll runs, and an account whose password was never saved (or whose keyring is locked) fails every unattended check after a relaunch, which an absent badge would otherwise report as a quiet mailbox. It sits on the **opposite corner** and is shown *beside* the count rather than instead of it: "3 unread, and the last check couldn't reach the server" is the reading that matters, and the failure is precisely what makes the count suspect, so suppressing either half destroys it. The tooltip spells out all three facts (unread · new · why the check failed). **Hovering the button reveals the accounts dropdown** — no second control in the header, the hover shape `LocalModelMenu`/`VpnIndicator` already have (the wrapper owns the handlers, so the pointer can walk down into the list; 250ms grace covers the gap; Escape closes one opened by keyboard focus). Hover is affordable here for the reason it was not on `MachinesIndicator`, whose menu had to be demoted to click: revealing this list **touches no network**. One row per account and one for **Important**/**Urgent**, each landing the overlay *on that list* in a single gesture (`stores/mail`'s `openAccountView`/`openPriorityView`, which open the overlay and select in one action so no frame renders the previous target). A click on the ✉ still just opens mail and takes the menu with it. Priority sits **above** Accounts and outside them, the rail's ordering and for the rail's reason; the counts are the same numbers the rail shows, refreshed **on reveal** (`refreshUnread` + `refreshPriorityCounts`, both local reads, and skipped for an already-open menu) rather than at mount, where they would be free to drift. Re-picking the account already showing a folder is deliberately a no-op: `selectAccount` re-opens the inbox, so a second click on the row you are reading would throw away the folder you navigated to — coming back from a priority list is the case that must still re-enter it. Untested. |
| `header/CalendarIndicator.tsx` | The header's 🗓 button, `MailIndicator`'s twin — built the same way for the same reason (a tab belongs to a scope; your calendar does not), but with **no experimental gate** above `calendar_global_app`, since the calendar is shipped and its store reads one local file. The one real difference is the badge: it is **derived, not acknowledged**. Mail counts arrivals and clears when you open it, because a delivery has then been seen; an appointment does not stop being at 3 p.m. because you looked at it — so this number is recomputed from the store on every edit *and* from the clock every minute (nothing in the store changes when 15:00 simply passes), and reaches zero on its own at the end of the day. The count itself is `stores/calendar`'s pure `eventsLeftToday` (tested in `src/__tests__/CalendarBadge.test.ts`): today's occurrences on *visible* calendars that have not **ended** — not-yet-ended rather than not-yet-started, so a meeting you are sitting in still counts. **Hovering reads the day out** (`MailIndicator`'s hover shape, and affordable for its reason — the store is already in memory and one local file behind it, so revealing the list touches no network): colour · time · title per occurrence, past ones **dimmed rather than hidden**, which is the agenda rail's rule and what stops the two surfaces disagreeing about what today held. Badge and list share ONE expansion — `stores/calendar`'s `dayAgenda`, which `eventsLeftToday` filters and `lib/todoBoard`'s `agendaWindow` now delegates to — because the list is the badge's explanation, and a "3 left" over four rows is worse than either alone; the same `now` feeds both, since two `new Date()`s a few lines apart can straddle a minute. **Tomorrow is a second section below today** — never merged into one list, where a 09:00 under a 16:00 reads as an ordering bug — and it renders even when empty, because "nothing tomorrow" is an answer and a section that vanishes reads as a list that failed to load. A row with a **video call** carries a **Join** button (`lib/conference.ts` + `linkTarget`'s `joinConference`), which is the whole reason to read the day from the header two minutes before the hour: the door, not the grid. It sits *beside* the row rather than inside it (a button in a button is invalid markup, and the two go to different places), and its tooltip **names the service**, because the link may have been derived from the event's location or notes. A plain row click just opens the overlay (the store has no per-day target to aim it at). Untested. |
| `header/TodoIndicator.tsx` | The header's ☑ button (`todo_board`), `CalendarIndicator`'s twin and badged the same way — **derived, never acknowledged**. The number is what today actually demands: open cards, on a visible calendar, due today or overdue. Not "every open card", which is a figure in the hundreds that never falls; undated cards are excluded for the same reason (a someday is not something today demands). Because it is derived it needs no dismissal — it drops as the day's cards are ticked and rises at midnight, which is why the clock is ticked once a minute as well. Overdue is emphasis on the same badge, never a second number. **Hovering reads the cards out** (`CalendarIndicator`'s hover shape, affordable for its reason — the tasks are in memory behind one local file, so a pointer crossing the header touches no network): three sections — **Overdue**, **Due today**, **Due tomorrow** — from the badge's own selection (`urgentTodos`, which `todosDueCount` filters the same way) and the same `now`, so no row can appear that the number did not count and neither can straddle a midnight. Tomorrow is a section *below*, never merged, for the calendar menu's reason; Overdue is split out because the badge's overdue emphasis has to be explainable by something on screen, and it is the one section hidden when empty (a standing "Overdue — nothing due" is the alarm word in the header for the normal state). An overdue row carries **how late in days**, which is the difference between yesterday and three weeks. A row opens **its own card**, not just the board (`stores/todo`'s `openCard` → `focusTaskId`, consumed once by `TodoPane`) — a list that has named the card and then drops you at a board of forty is where this would still cost a search. |
| `header/VpnIndicator.tsx` | Machine-wide OpenVPN control: always present, lists stored `.ovpn`s, connects/disconnects a tunnel with or without a project behind it, names its holders, and arms one to connect on launch. |
| `header/WindowControls.tsx` | Minimize/maximize/close window buttons. |
| `tabs/TabBar.tsx` | Per-subwindow tab strip (add/rename/close, pointer-based DnD). |
| `tabs/Subwindow.tsx` | A single tiled subwindow (tab group). Its body is a row: the measured pane region + (when the group's `filesOpen` is set) the docked per-subwindow file viewer. |
| `tabs/agentModes.ts` | Planner/doer capability table: which agents can be launched into a Plan/Auto mode **and** resume on the respawn that costs (Claude + Gemini), plus the idempotent arg rewrite. |
| `tabs/commitDrop.ts` / `tabs/commitFileDrop.ts` | Apply a tab/file drag-drop into the layout tree. |
| `tabs/dragGeometry.ts` | Drop-zone/split geometry math for tab drags. |
| `terminal/TerminalView.tsx` | xterm.js terminal wrapper + PTY I/O. |
| `files/FileTreeSearch.tsx` | The flat result list `FileTree` shows in place of the browsed listing while its search box holds a query. Two modes: **name** (literal case-insensitive substring match on paths from `list_project_paths`, basename hits ranked first — deliberately NOT a fuzzy subsequence, which matches nearly everything) and **content** (literal line search via `project_search`, the same backend the right panel's Search view uses). Every result does both **jump-to-path** (reveal + select in the tree, via `FileTree`'s `revealPath`) and **open** (content hits open at the matched line). Confined to the browsed folder by default (`scopeRel`) with a **root** toggle for the whole project — content search walks only the scope; filename results filter to it. Local-source only — both backends walk the canonical local path — so `FileTree` only shows the box for a non-remote-source tree. |
| `files/FileTree.tsx` | Project file tree with git markers, fs-watch refresh. Hosts the in-tree search box + `FileTreeSearch` results and owns `revealPath` (jump-to-path: navigate to a rel path's folder, select + scroll the row). Owns the file drag-and-drop: pointer-drag a file onto a tab bar (→ embed/split/popout, via `stores/drag` + `commitFileDrop`), onto a folder row to move it, ctrl+drag to export it to the OS. Its "Open in a new tab" is a *host* callback (`onOpenFolderTab`), not a store call — a tree with no way to own a tab (a box root, a popout running on a streamed copy of the tabs) simply doesn't pass one, instead of offering an action that would go nowhere. |
| `files/ProjectFilesPane.tsx` | **The** project file view — tree, sort row, remote sync row, Downloads — rendered *twice*: by `RightPanel` and by the Files (Project) tab. One component, so the panel and the tab can never drift into two different file views of the same project (the bargain `PageStrip` strikes between the print preview and the PDF rail). What each host still owns is exactly what must differ: the browsed folder (two views of one project must not yank each other around), the sort (the panel unmounts the pane for Git/Search and the order has to survive the trip back), and where the Remote/Local switch sits. Also home to `useFileSource` / `useIndependentFileSource` — the Local/Remote side, which is decided **once** and thereafter moves only because the user clicked the switch. The usable side is *latched* into `stores/fileSourcePref` the first time the SSH lamp reads something settled (connected → the host tree, anything else → the mirror, so a disconnected remote never opens on a Connect prompt), and a latch is never overwritten. That is the whole point of the store: the side used to be re-derived in a plain mount effect, so every remount of a file view — hiding and re-showing the panels (the right panel is unmounted, not hidden), a scope switch, an activation that briefly cleared the active project — silently threw a deliberate "Local" away the moment the pool was up, and the tree jumped to Remote with nothing touched. The per-viewer half (each Files (Project) tab, each subwindow's docked column) had the same bug one level down: its choice lived in component state and its host remounts it by `key={scope}`, so it came back re-seeded from the project-wide value — hence a `viewerId` (`group:<id>` / `tab:<key>`) and a session-scoped row in the same store. An explicit project-wide click is persisted to localStorage; latches and per-viewer rows are session-only. Plus `useRemoteBlocked` (the gate every SFTP/git probe needs: they are synchronous Tauri commands, so dispatching one at a dead session freezes the window). |
| `files/ProjectFilesView.tsx` | **The** whole file *viewer* — view switcher (Files / Git / Search / Apps / Orange), inline git action bar + `GitHistory`, `SearchPanel`, tracked-windows list, diverged-files (orange) list, type tags, hover card, SSH logout, import / open-in-OS / downloads / settings — wrapped around `ProjectFilesPane`. Rendered *twice*, by `RightPanel` and by the Files (Project) tab, so the panel and the tab can never drift into two different file *viewers* (the same bargain `ProjectFilesPane` strikes for the tree). Each host owns only what must differ: identity (panel = active project, tab = its own scope), the browsed folder, the `useFileSource` hook, and the panel-only window chrome — injected via the `resizeHandle`/`pin`/`hidden` ReactNode slots, so a tab (which has none) simply passes none. `active` gates git/windows probes + the pill dirty-dot write (a closed panel / background tab never churns); `mountTree` keeps the tree + fs-watch alive (a background tab keeps its tree even when not probing). Also hosts the **HPC** surfaces (`docs/hpc_workspace_plan.md`): the Jobs view's workspace list (remaining time toned, **Extend**, **Move here**, **Pull logs**) and the **expiry banner** — raised in *every* view, not just Jobs, because for a project whose root lives in a workspace an expiry deletes the host tree itself, and a warning nobody navigates to is not a warning. The workspace read therefore also runs outside the Jobs view whenever the project records a workspace. |
| `files/ProjectFilesTab.tsx` | The Files (Project) tab: a thin host that renders `ProjectFilesView` (so it has *exactly* the panel's features). It resolves the project from its own `scope` and keeps the browsed folder on the *tab* (`TabEntry.folder`, persisted), which is what makes "Open in a new tab" on a folder mean anything after a restart. Passes no window-chrome slots. Also exports `openProjectFilesTab` (kept here to avoid a `ProjectFilesView`↔tab import cycle). |
| `files/SubwindowFilesSidebar.tsx` | The per-subwindow right file viewer (◫ on every tab bar, main window and popouts): a resizable column docked on a subwindow's right edge, hosting the SAME `ProjectFilesTab` host the Files (Project) tab is — so all viewer surfaces stay one component. State (`filesOpen`/`filesWidth`/`filesFolder` — open flag, column width, and the browsed folder) lives on the layout tree's `GroupNode`, so it persists with the layout, travels with a detach, reaches a popout via the streamed `files` detached edit, and is freed when the subwindow (group) is dropped; the sidebar renders BESIDE the measured pane region, which is what keeps the main window's flat pane layer from painting over it. |
| `files/importDrop.tsx` | OS-file drop → copy into the project, with the collision prompt (`useImportDrop`). Shared by the panel and the tab so an import behaves the same wherever it lands. WebKitGTK withholds dropped paths from HTML5 drops and only leaks one via `text/html`, which is why the picker (`⬇`) — not the drop — is the reliable multi-file route. |
| `files/ProjectFilesSettings.tsx` | The tree's hide-these-endings lists (read from / written to the project's own `project.json`, hence shared by both file views) + the Project Settings dialog that edits them. Also the **Alerts** opt-in (`files_alerts` + its lookahead and per-source toggles), and the **Unmute all** row — shown only when `files_alerts_muted` is non-empty, and deliberately naming the *raw* stored count rather than the group's live one, since this is the only control left for a mute whose row no longer renders. Built from `layout/settingsUi.tsx` like every other settings surface; its one local piece of CSS is the **native-viewer table**, whose rows are a fixed five-column grid with a header row — a type without local completion renders three empty cells rather than a shorter row, which is what keeps the switches readable as columns. |
| `files/AlertsSection.tsx` + `files/useAlertsFeed.ts` | The **Alerts group** below the tree (group R #67): urgent mail, the next appointments and the cards due or overdue, in one time-ordered strip. It is in the file viewer because that is the surface open all day — the three sources each have a home, but all three are places you have to *go and look at*. **`files_alerts` IS the visibility** (default **on**), not a preference above a separate shown flag: the toolbar 🔔 writes that key, so a close persists instead of returning at the next remount — and this viewer is mounted many times over at once, so a per-surface flag would have to be dismissed once per surface. The 🔔 renders whether or not the group is on, because it is the way back and gating it on the same key would make the × a one-way door. It is **not** shown in the docked subwindow column (`compact`) at all, and neither is the group: that viewer is a ~300px sidebar beside a terminal, where the strip takes the space the tree is there for, and it is the surface mounted many times at once, so one alert would be repeated once per open subwindow. The 🔔 goes with the group rather than staying as a toggle for something that can never appear there — it writes a global key, so it would silently arm the group in the right panel and every Files tab instead. Off is a real off: the hook arms no timer, runs no read and collapses every store selector to a frozen empty. `useAlertsFeed` owns the reads (mail gated again by `mail_client`, *before* the invoke, since opening the mail store creates its database) and the RRULE expansion, which is `lib/recurrence`'s and contributes **one row per series** — a daily stand-up would otherwise fill a twelve-row strip and collide as React keys. Rows route to the surface that owns the item and nothing is *completed* from here: an alert clearable in two places would disagree with itself. **How far off a row is comes from `lib/todoBoard`'s `awayDelta`**, the measurement the board chip and the header's to-do list already print, not a second rounding here — this strip used to roll everything past a day down to whole calendar days, so a card the board called "in 2d 5h" was "in 3 d" two centimetres away. A timed row (an appointment, a mail, a card with an **hour** deadline) therefore keeps both halves. Which rows keep their hours is `readsInHours`, **not** `allDay`, and the two part exactly at the date-only **card**: a card with no time still has a deadline — the midnight its day runs out at, already the instant `minutesAway` counts to — so it is read in days *and* hours, where the whole-day reading said a bare "today" for the entire last day and never how much of it was left. An all-day **event** keeps the whole-day reading, because a conference day is a fact about the calendar rather than an hour to make and "in 9 h" is a precision it does not have. The wording stays this group's own (`filesAlerts.*` — spaced units, "overdue" rather than "late"), so only the unit is shared, never the phrase. **Muting is the exception and is not a dismissal** — a row's 🔕 hides that row in this group and touches nothing behind it (the mail stays marked, the card stays due), so there is no second truth to disagree with; it exists because the strip is twelve slots and one standing item you have already decided about costs a slot every day. The ids live in `files_alerts_muted` (settings, not component state — the viewer is mounted many times over, and a mute that came back at the next launch would be a control that doesn't work) and the mute is applied **inside** `selectAlerts`, *before* the cap, or three silenced meetings would hide an overdue card. It is reversible from two places: the header's 🔕 N reveals the still-live muted rows, each with its own unmute, and the Project Settings dialog names the raw stored count with "Unmute all" — the one control left for a mute whose row is gone. The list is **bounded** (`MAX_MUTED_ALERTS`), never pruned: an id with no row today may well have one tomorrow. Mail and cards open *aimed* (`selectMessage`, `openCard`→`focusTaskId`); an event opens the calendar overlay plainly, because `stores/calendar` has no per-event focus request and inventing one was not this feature's to do. |
| `files/SendToProjectDialog.tsx` | The file-tree right-click **"Send to project…"** flow (`lib/sendToProject` remembers the last target): pick one of the active projects, browse the folders inside it (project-confined via `list_dir`, so the browse can't escape the project), and copy the file/folder there via `import_external_file`. Non-destructive — the source stays put, a name collision keeps both (`import_external_file` appends " (n)"). **Local destinations only for now**: a remote project's tree lives on the host and only its mirror is local, so remote rows are shown disabled rather than landing a copy in an un-synced mirror. Only offered on a *local* source row (a remote SFTP listing's `entry.path` is not a filesystem path). Reuses the `folder-picker-*` chrome. |
| `files/FileBrowser.tsx` | The *other* files tab: a two-pane explorer (list/icons, columns, history). Deliberately not the tree — no drag-and-drop, no git markers; both are offered. |
| `files/GitHistory.tsx` | Commit history / commit / push UI, the lockstep bar, and the **Worktrees** section (#23). Two rules there are the frontend's own. The branch control **swaps by mode**: a `Dropdown` of existing branches when checking one out, a free **text input** plus a start-point picker when creating one — a listbox in both modes is what made the new-branch toggle unable to ever succeed, since `-b <an existing branch>` is precisely the one thing git always refuses. And a removal is a **confirmed, escalating** action: `git worktree remove` refuses a dirty tree but does *not* protect ignored files, so it deletes a `node_modules`, a `.venv` and a `.env` wholesale with no undo — hence the confirm naming the path, and a re-offer at exactly the force level git itself names in the failure (`use --force` → 1, `use 'remove -f -f'` → 2). Branches already checked out anywhere are filtered out of the create form (`Worktree.branch` and `GitBranch.name` are the same string space), the worktrees root is derived from the **main worktree's own path** in the listing rather than from `projectDir` (they are two machines' paths for a remote project), and the three git reads are `Promise.allSettled` so one failing probe cannot blank the whole view. |
| `files/SetDefaultAppDialog.tsx` | Pick the default app for a file type. |
| `embed/EmbedPane.tsx` | Hosts an embedded external app window. |
| `embed/FileViewerPane.tsx` | In-app viewers (image, markdown, code, TeX/SyncTeX) + shared viewer plumbing. |
| `embed/YamlTree.tsx` | The YAML/JSON structure tree: the editable half of the viewer, the way the rendered preview is markdown's. It renders *rows*, but it edits *text* — every action splices the draft `TextView` already owns, so Tree and Source need no conversion between them and a tree edit is an ordinary dirty/undoable/saveable change. A flow (JSON) container is badged as such, because that is the style its edits will keep. A key wears its `#` comment on its **hover** and a `#` marker when it has one (a config file's comments are the half worth reading, so they are not hidden behind a hover — only their text is). Reordering is a **pointer** drag from the row's grip (HTML5 DnD does not work under WebKitGTK, and mid-gesture listeners get dropped, so every handler is bound to the grip up front); it lives in `YamlRows` because a node can only be reordered **among its siblings** — dragging a key into another mapping is a re-parent, not a reorder. Every entry also has a **copy** button feeding an app-wide buffer (plus the system clipboard as text) and a click-placed **paste cursor**: the cursor only lands where the entry *fits* (`canPasteAfter` — mapping entry into a map, item into a list, block/flow never mixed), so a paste can never tear the file. |
| `embed/BibCards.tsx` | The BibTeX (`.bib`) **card view** — one card per bibliography entry, its `field = {value}` pairs as rows, in one long scrolling column, with Source as its other half. It is `YamlGrid`'s shape **minus the drill**, and that is the whole design difference: a `.bib` is not a nested document but a *flat list of records* all at one level, so there is nothing to drill into and the reader wants the list itself. What a flat list of a few thousand records needs instead is a **filter** (over the key, the type and every field value — a real library is thousands of entries) and a **per-card fold**, and the fold rides with the tab (`ViewerState.bibCollapsed`) because a fold that came back on the next open is a control that does nothing; the filter deliberately does *not* persist, for the to-do board's reason. Collapse/expand-all applies to what is **filtered in**, never to the 1900 cards off screen. Like the tree and the table it renders controls but edits *text*: every action splices the same draft via `lib/viewers/bib`'s ops, so a card edit is an ordinary dirty/undoable/saveable change and the file's field order, brace protection, `"…"` quoting, alignment and `%` comments survive it. Two honesty rules are inherited: a value it cannot rewrite safely is **shown, not offered** (in full, unellipsized — this view is the only place its source form appears), and text belonging to no record is **admitted in a note** rather than silently omitted. The card head's caret is its own button with the type/key inputs as siblings — nesting a text field inside the fold button would be invalid markup *and* would fold the card on every click meant for the key. Three further controls exist because a real library is *read as a list*, and all three are **display-only** — none rewrites the file's own order: a **venue** picker (which conference/journal — a question the text filter answers badly, since `neurips` also matches every abstract that mentions it, and a `journal = jml` macro is resolved through the file's `@string` table so the picker offers the journal rather than the macro's name); a **sort** (file order · first author · year, either direction), persisted as `ViewerState.bibSort`/`bibSortDesc` because unlike a filter an order hides nothing; and **lazy rendering** — a card is a dozen inputs and a growing textarea per field, so only a *window* of the list is mounted (`BIB_PAGE`, extended by an `IntersectionObserver` sentinel that doubles as a "Show N more" button, since the pane's own body is the scroller and there may be no observer at all in a test). A window rather than a growing prefix because of the one action that must reach a card nobody scrolled to: adding an entry appends it to the end, and revealing the 1900 cards above it to get there would pay exactly the cost the window exists to avoid — so the window jumps to it and offers "Show N earlier" back. Untested. |
| `embed/YamlGrid.tsx` | The YAML/JSON **card view** — an *optional* view for structured YAML/JSON, beside Tree (the default) and Source (#yaml-grid; the toggle reads "Cards"). A **drill navigation, not an infinite nest**: one card is "main" at a time; the grid shows a breadcrumb from the document down to it, the main card's *level* (its siblings, so you can step sideways without going up first), and the main card's *children* as the next level below. A card shows only its own **scalar fields** — its nested collections are reached by clicking its title (or its "N groups" footer), which opens it as the new main and reveals them as the next level; a card with no nested collection gets a static (non-clickable) title. With nothing focused the grid is the top overview: a root map is one `document` card, a root list is a card per item. A list item's card is titled by its name-ish field (`name`/`id`/…) or `Item N`, never `#0`. **Cards reorder by dragging** among their siblings — pointer-based from a grip (HTML5 DnD is dropped by WebKitGTK) — committed as one `moveNodeTo` splice, exactly as `YamlRows` does. Like `YamlTree` it renders controls but edits *text* — every action splices the same draft via `lib/viewers/yaml`'s ops (`setValue`; `addChild` for `+ field`/`+ card`/`+ value`; `deleteNode`; `duplicateNode`; `moveNodeTo`), so a card edit is an ordinary dirty/undoable/saveable change and Cards/Tree/Source are three views on one text. A value it can't safely rewrite is shown, not offered (`source only`). `TextView` opens YAML in **Tree** by default; the Cards toggle appears only for a file that nests something to card (no `hasCards` ⇒ hidden), and the card view still needs work. |
| `embed/TableView.tsx` | The CSV/TSV table (and, read-only, the `.xlsx` sheet). Like `YamlTree`, it renders *rows* but edits *text* — a cell edit splices the draft `useEditableFile` owns, so it is an ordinary dirty/undoable/saveable change and every cell nobody touched keeps its bytes. Hence sort and filter carry each row's **source** index (`RowRef`): a splice must address the row a cell came *from*, not the row it sits on. The separator is a **guess the reader can overrule** — sniffed, then offered in the header (Auto/comma/semicolon/tab/pipe/custom); an override persists per tab, Auto does not. Columns hide by name from `ColumnsMenu` (click to hide, click again to show; multi-select, so it stays open), which is why the row filter searches only the **visible** columns and why hiding is dropped when the delimiter changes — a hidden column is an *index*, and another separator cuts the row into different ones. Only the visible rows render, which is why the column widths are measured over the whole file up front: sized to what is on screen, every column would resize as you scrolled. |
| `embed/GifView.tsx` | The animated-GIF viewer (#gifviewer): the plain image viewer already *animates* a GIF (the webview animates `<img>` natively) but offers no control, so this one decodes the frames itself (`lib/viewers/gif.ts`) onto a `<canvas>` and adds the transport — play/pause, frame stepping, scrubber, speed, loop, frame/delay readout — over the image viewer's exact zoom/pan (shared exported pieces: `zoomOffset`/`clampScale`/`useNonPassiveWheel`). Playback is a rAF timestamp accumulator, so per-frame delays are honored without drift; authored sub-20ms delays play at the 100ms browser convention. A GIF the decoder can't handle degrades to the native animated `<img>` — never worse than before the viewer existed. No Annotate: flattening to PNG would silently destroy the animation. |
| `embed/pdf/PdfViewer.tsx` | The PDF viewer: pdf.js canvas stack, zoom/find/print toolbar, SyncTeX, page rail, the **contents** sidebar (chapters, ☰ at the toolbar's far left) and the **link layer** (`links.ts`) over every page of the file itself — **painted**, in the colour of what each link is (cross-reference · citation · external address), because a paper compiled without `colorlinks` says nothing on the page about the "(3)" after "see equation" being clickable, and links found only by sweeping the pointer across the page save none of the work they exist to save. The mark is an underline plus a wash rather than a filled block (the box is the glyph run's own rectangle, so a solid tint reads as a highlighter pen through the sentence) and the colours are fixed rather than the theme's accents, since they sit on the document's white paper, not on app chrome. Following an internal link lands on the destination's own line, marks it, and pushes the previous position on a bounded back stack (`←` / `Alt`+`←`); a Ctrl/⌘-click on a link box is still SyncTeX reverse search, measured against the canvas rather than the event's target, because the modifier is the reverse-search gesture over the whole page. Renders an *arrangement* (`pageModel`), not the file — so reorder/delete/turn/merge are array ops, and nothing is written until Save. The **blackout tool** (▮, #pdf-redact) is the same bargain: a drag over a page marks an area (snapped out to whole words, `lib/viewers/redact`), "black out all N matches" marks every Ctrl+F hit from the measurement the highlight is already drawn from, and both are ordinary undoable edits until Save burns them in. Three rules are the viewer's own. A mark is painted **solid black whether or not the tool is armed** — it is a preview of the saved page, and hiding it behind an editing mode would hide exactly what is about to be destroyed (the rail thumbnails paint it too, and print rasters carry it, or a print would leak what a save destroys). The **drag layer covers the page** while armed, which is what takes the gesture away from the link boxes underneath. And the Save that flattens is **confirmed**, naming areas *and* pages, since the same button also writes ordinary page moves. The **metadata panel** (🏷, #pdf-meta) is that tool's twin on the file's other half — what the document says about itself, none of which is on any page. Three rules. It **shows the fields before it offers to delete them**, because a bare "Delete all metadata" button acts on something the reader can neither see beforehand nor verify afterwards; the read is `readPdfMetadata` (pdf.js, so the panel and the file in front of them cannot disagree) and it runs **on open**, not at load, so a reader who never opens it pays nothing on the reload of every recompile. It is **pending, not immediate** — one flag on the save rather than an arrangement edit, since there is no page it belongs to — which is what makes it as cancellable as everything else here and is also the *only* thing that makes Save reachable on an otherwise untouched file (`dirty` reads it). And the delete is offered even for a file listing nothing, because the list is what pdf.js can see: the per-page packets and the private scratch space an editor left behind are cleared by the same save and are not in it. Pending deletion travels with a page dragged out, for the blackouts' reason. The text-run cache the snapping needs is keyed on `src:page:rot` rather than on the arrangement object, or drawing a box would re-read every page's text content once per box. Owns the **spring-loaded rail**: while a page drag is in flight anywhere, every open PDF opens its rail and puts it away again afterwards unless pages actually landed in it — so a document that was merely *open* becomes a destination, instead of the feature requiring each end to be armed by hand first. The subscription lives here, on the **viewer**, and that is the whole point: the strip only exists while the rail is open, so a strip-level one could never hear the drag that ought to open it. Two orderings are load-bearing and both are commented at the site — `onImport` clears the auto-opened flag **synchronously** (the drop must win against the close), and the close is deferred a macrotask because a cross-window drop reaches this listener and the strip's on one event in registration order, not in dependency order. **Remarks** (💬, #pdf-notes) are the third pending edit and the only *additive* one: a **right-click on any page** offers "Add remark here", a marker lands where the pointer was, and the card that opens writes a real PDF sticky note — read back out of the file's own `/Text` annotations (`notes.ts`, lazily per page, on the link layer's gate and for its reason) and written back into them at Save (`pdfDoc.ts`), so what is saved is a comment every other reader shows. It needs no armed tool, unlike the blackout beside it, because a comment is something you do while reading rather than a destructive edit that wants a visible mode. Two things about it are load-bearing. The page's own remarks are the **baseline** an edit adopts (`PageRef.notes` is all-or-nothing), so the menu's action stays disabled until that read has landed — offering it early would take "this page has no remarks" as the baseline and delete the file's own comments at the next save. And the card **commits once** rather than per keystroke, or writing a sentence would cost a sentence's worth of undo steps. The pending readout beside Save counts *sheets*, not remarks, since deleting the last remark on a page leaves nothing to count and is still a change waiting to be written. Remarks are also **written on their own** (`NOTE_AUTOSAVE_MS`, per tab via `ViewerState.pdfAutosaveNotes`, default on): a remark is something you write while reading, and the reader who wrote one has said what they meant to. The whole safety of that is in what it refuses to carry — a save writes the *whole* arrangement, so an autosave firing with a page move pending would commit the move and one firing with a blackout pending would **flatten the sheet**, the single irreversible edit here and one that is deliberately confirmed; hence `isPristineExceptNotes`, plus a file that has not changed underneath and no pending metadata deletion, and a panel that says so when it is holding rather than leaving a switch that is on and quietly doing nothing. A silent save also **does not reload the document**, which is the point of it: a reload re-parses the file, repaints every canvas, resets the scroll, clears the undo history and re-mints every remark id — a visible event, once per comment, in the middle of reading. It reconciles in place instead (the written sheets give up their `notes` ownership and what was written becomes the file's own set in the cache, under the SAME ids), which is safe precisely because the gate above admits nothing but the identity arrangement, and a sheet edited while the bytes were in flight keeps its ownership because its `notes` array is no longer the one that was written. The Save button's spinner stays down for it, too — that spinner is feedback for a click. The file's own remarks are read **here**, not in the page canvas, keyed by source/page/rotation: a read mints fresh ids (an annotation reference is document-scoped and rewritten on every save), so a page reading its own set while the panel read another would give two sets of ids for one comment and "go to this remark" would address one the page had never heard of — one owner, one set, requested by a page when it comes near and by the panel for the whole document when it opens. **Selecting text** (#pdf-textselect) is no longer a tool at all: `PdfTextLayer` is up on every near page and the plain drag selects, with everything that needs its own click stacked above it by z-index (the arrangement pdf.js's own viewer uses). A selection raises `PdfSelectionBar` — four highlighter colours, "highlight and write a remark", and the copy chip — and, by default, puts the words on the clipboard by itself (`ViewerState.pdfCopyOnSelect`, per tab, turned off from that chip; written on `mouseup`, never on `selectionchange`, which fires on every pixel of a drag). A **highlight** is a remark with `quads`, so it rides every piece of machinery remarks already have; a drag across a page break becomes one highlight per sheet, because an annotation belongs to a page. Every **page** render (the reader's canvases, and the redaction raster in `pdfDoc.ts`) runs at `AnnotationMode.ENABLE_STORAGE` for one reason: a highlight the viewer has read is drawn *by the viewer*, so the file's own copy is suppressed with `{ noView: true }` keyed by the annotation id — and the map of what is suppressed is held on its own rather than derived from the remark cache, since an autosave replaces that cache with notes carrying no source id and a derived key would empty itself, bringing the file's originals back underneath ours. |
| `embed/pdf/pdfDoc.ts` | The open source documents an arrangement draws from, and `buildPdf` — the ONLY place a PDF is written (pdf-lib, on save). Also the **blackout burn-in** (#pdf-redact): a sheet carrying marks is not copied at all but *flattened* — rendered to pixels, the marked areas painted out of those, and the image made the whole page. That asymmetry is the feature's entire security claim: a rectangle drawn over text hides nothing, because the glyphs stay in the content stream and come back out of any copy, extract or annotation delete. The cost is the rest of that page's text, vectors, links and tagging, which is why only marked sheets take this path and every other page is copied across intact. The gentler alternative — editing the content stream to drop just the glyphs inside each box — is deliberately not on offer: doing it correctly means tracking text state, font metrics and every form XObject well enough to know where each glyph lands, and a redaction that is subtly wrong is worse than one that is heavy-handed. `rasterizeRedactedPage` is injectable (`BuildPdfOptions.rasterize`) so the surgery is testable without a canvas. Also **deleting the metadata** (#pdf-meta, `BuildPdfOptions.stripMetadata` + `readPdfMetadata`): what the file says about itself as opposed to what it shows, which lives in three separate stores and needs a different answer for each. The `/Info` dict is never *created* — pdf-lib writes its own Producer, Creator and a `CreationDate` of **now** on every `PDFDocument.create()`, so a strip declines it at the source (`updateMetadata: false`) rather than deleting it afterwards, which would leave the dict serialized in the bytes as an orphan. The catalog's XMP never comes across at all, the output being a fresh document. What genuinely does is the **page** level, since `copyPages` brings each page dict over as it stands — so `/Metadata`, `/PieceInfo` and `/LastModified` are deleted there. And deleting the key is not enough on its own, which is the subtle part and the reason `collectGarbage` exists: pdf-lib serializes every object registered in the context, reachable or not, so an XMP stream whose last reference has just been removed would still be written into the file in full — deleted from the structure and perfectly readable in the bytes, the same shape of failure as a rectangle drawn over live text. Hence a mark-and-sweep from the trailer before `save()`, which is safe by construction (anything the catalog reaches is kept) and runs before the flush that registers a redacted sheet's images. `PdfSave.test.ts` asserts against the **saved bytes** for that reason, never against the object model. Also **writing the remarks** (#pdf-notes, `writePageNotes`): a sheet the arrangement has taken over gets its `/Text` **and `/Highlight`** annotations replaced by that sheet's remarks, and every other annotation on it — an underline, a strikeout, an ink scribble, a stamp, and its links above all — its links above all, which is most of what a `hyperref` document carries — is left exactly as copied. A dropped note takes its `/Popup` with it, or a reader draws the orphan as an empty floating window. The array is **rebuilt** rather than edited in place because pdf-lib's copier maps one source object to one copy, so a duplicated sheet shares its twin's `/Annots` and an in-place edit would write one page's remarks onto both. `Contents` is a hex string (UTF-16BE), since a literal mangles everything outside PDFDocEncoding; `/F` is `Print` and nothing else, `Hidden`/`NoView` being how a comment goes missing and `ReadOnly` being how the recipient's own viewer is stopped from answering it. A **flattened** sheet keeps its remarks though it keeps nothing else — the blackout destroys what was on the page, and a comment about it is the reader's own work, written afterwards in a space the rasterisation leaves unchanged. `noteViewport` is injectable for `rasterize`'s reason. |
| `embed/pdf/outline.ts` | The contents/chapters model (#pdf-outline), pure. `loadOutline` resolves the PDF's **embedded** outline (bookmarks) to 1-based file pages (named + explicit dests); `detectHeadings` is the **font-size fallback** used only when there's no embedded outline — it infers a heading tree from the text's typography (body-size mode, line grouping, running-header drop, nest-by-size). Jumps map a file page to whichever sheet shows it, so they survive page rearrangement. Also home to `resolveDest`/`destTop` — the viewer's **one** destination resolver, shared with the link layer, so a chapter and a `\ref` to the same anchor cannot disagree about where it is; `PdfDest.top` deliberately stays in the file's own (bottom-left, unrotated) units, because converting it needs the turn the viewer has applied to the *target* sheet and only the jump knows that. Tested in `src/__tests__/PdfOutline.test.ts`. |
| `embed/pdf/pageFingerprint.ts` | **What makes a recompile quiet**: a cheap content fingerprint of one page, so a reload can tell "this sheet is different" from "this sheet is byte-identical to what is already painted". A LaTeX build rewrites the whole PDF for a one-word edit, so the viewer used to repaint every page on screen — the flash on every compile, and seconds of rasterisation for pixel-identical pages. It is taken from the page's **operator list**, the drawing instructions pdf.js would execute, not from its extracted text: text alone misses a redrawn figure, a moved rule or a changed font, while the paint program changes whenever a pixel would. The one thing it cannot see through is raster image **data** (an image is referenced by object id, so a bitmap swapped for another of identical size at an identical position fingerprints the same) — the deliberate limit, and why `samePage` is only ever used to skip a repaint of an **already-painted** page, never a first paint. A null fingerprint (a pdf.js error, a file the compiler is still writing) never compares equal, so the caller falls back to painting. The other half of the fix is not here: `PdfPageCanvas` renders **off screen and blits**, since sizing the visible canvas clears it and left the page blank for the length of the render, and `pageModel`'s `keepPageIds` is what keeps the canvases mounted across a reload at all. Tested in `src/__tests__/PdfQuietReload.test.ts`. |
| `embed/pdf/links.ts` | The PDF's **own hyperlinks** (#pdf-links) — what `hyperref` ships for every `\ref`, `\cite`, `\autoref` and contents row, plus any `\url`. Two actions are honoured and the omissions are the design: a **GoTo** (internal, resolved through `outline.ts`) and a **URI** (external, confirmed before it opens); a form widget, a `Launch` naming a local program, a `GoToR` and a named action are **not rendered at all**, because an inert box reads as a bug and a box that starts a program is a hole. `url` and never `unsafeUrl`, so an address pdf.js already refused arrives with nothing to click and `routeUri` checks the scheme again — two gates, neither trusting the other. Rects come out in the {@link SyncRect} space the SyncTeX highlight and the Ctrl+F hits already use, so a link box is positioned by the one existing `bigPointsToCssRect` and follows zoom and rotation for free. Destinations are cached weakly per document — a bibliography page points a hundred links at a handful of anchors. An internal link also carries a **role** (`destRole`), which is the only thing its colour is drawn from: a citation is told from a cross-reference by the anchor `hyperref` writes for one (`cite.<key>`), and by nothing else — guessing further from the target page would colour a `\ref` to the last appendix as a citation. Tested in `src/__tests__/PdfLinks.test.ts`. |
| `embed/pdf/notes.ts` | The PDF's **own remarks** (#pdf-notes) — the `/Text` annotations every other reader shows as sticky notes, **and** the `/Highlight` ones it shows as marked text. `links.ts`'s sibling, deliberately: same page, same annotation array, same {@link SyncRect} space, read for different subtypes, so a marker follows zoom and rotation through the one existing `bigPointsToCssRect`. Reading `/Highlight` is what the text layer bought: this module used to ignore the subtype and said why — a highlight is painted by the page render itself, so surfacing it here would draw it twice, and a viewer with no text layer cannot know which words one would cover. Both halves are now answered (the words come from the selection; the doubling is settled by suppressing the file's own paint through pdf.js's annotation storage, keyed by the id kept on the remark as `srcId`), so exactly one thing on screen draws each highlight and it is the one that can be clicked. An underline, a strikeout, an ink scribble or a stamp is still the page render's and still has no editor — the same rule rather than an oversight: what this module reads is what a save rewrites, and a subtype nobody here can edit is one a save must not touch. `quadPointsInPdfSpace` writes the corner order every real producer and reader uses (TL, TR, BL, BR), **not** the spec's counter-clockwise-from-lower-left wording, which half the readers in existence draw as a bow tie. A note with **no text is still returned**: it is in the file, and hiding it would leave the reader unable to delete a marker every other viewer shows them. The write side is here too — `noteRectInPdfSpace` converts an anchor back into the page's own user space by mapping **both** corners rather than an anchor plus a size, since a quarter turn swaps the axes and would otherwise put the icon box on the wrong side of the anchor. `NOTE_ICON_PT` is 22 because that is what pdf.js normalises an appearance-less note's rect to, so a save followed by a reload puts every marker back where it was. Tested in `src/__tests__/PdfNotes.test.ts`. |
| `embed/pdf/PdfNoteLayer.tsx` | The remark markers, the **highlights**, the right-click menu that places one, and the card a remark is written in. A remark is drawn as one of two things and the model says which (`PdfNote.quads`): a sticky note is a pin at a point, a highlight is the boxes it covers — the only branch in the file, since the card, the menu, the dismissal rule and the panel's flash are written once for both. What the branch changes is real: a highlight has no drag (it is pinned to words), it has a colour worth changing from its menu, its card opens *under* the sentence rather than beside a pin (a card over the words is the one thing it must not be), and clearing its text does **not** delete it — marking a sentence is a complete act and the remark was the optional half, so deleting one stays a deliberate menu action. The gesture is the one every other PDF reader has — **right-click the page, add a remark** — with no tool to arm and no mode to leave, which is the deliberate difference from the blackout tool next door: a blackout is destructive and wants a mode you can see you are in, a comment is something you do in the middle of reading. Purely presentational (every piece of state belongs to the page canvas, so one owner decides what has the pointer over a sheet). Three rules are its own. A marker is a **fixed size**, not scaled with the page — it stands for something somebody wrote rather than covering an area, the same thing a PDF's own `NoZoom` flag asks for. The card **stages the text and commits once**, or every keystroke would be its own undo step — **Enter** is what writes it (Shift+Enter is left as the new line, ⌘/Ctrl+Enter still works) and Escape discards a draft. A press **anywhere else puts the card away**, the menu's own dismissal applied to it, and what happens to the draft is the decided half: a card holding something the file does not is **committed** rather than dropped (a stray click must not cost a sentence, and Escape remains the way to discard one), while a card that was merely opened and read only closes — a commit stamps `modified` and marks the sheet edited, and looking at a remark is not editing one. Pressing a *second* marker dismisses by the same rule, since a marker claims its own press and the window listener never hears it. And an **empty remark is not a remark**: saving one empties it out of the file, and a draft never typed into is dropped, because a blank marker is indistinguishable from a bug in whatever viewer opens the file next. The menu withholds "Add remark here" until the page's own remarks have been *read*, which is not cosmetic — that list is the baseline an edit adopts, and placing one early would silently delete the comments already in the file. A fourth rule arrived with the **move**: a marker drags to a new anchor, because a right-click places a remark where the pointer was and where the pointer was is regularly a line off — without it, correcting one meant deleting the remark and retyping it. The drag shares the marker with the click that opens the card, so **distance** tells them apart (`NOTE_DRAG_SLOP`, in CSS pixels, so it means the same at 40% and at 400%) and the click is swallowed only when the pointer travelled; `pointercancel` commits like `pointerup`, the trap the tab and card drags document; and `clampNoteAnchor` clamps by the icon's own box rather than by the anchor, since a remark parked half off the sheet would still be *drawn* here and would carry a `/Rect` outside the media box for every other reader to place as it likes. It also draws the **flash** the remarks panel walks by, and closes a card whose remark is no longer on the sheet — which is an ordinary event now that autosaving re-reads the file and re-mints every remark id. |
| `embed/pdf/PdfNotesPane.tsx` | **The remarks panel** (#pdf-notes) — every comment in the document in one list, and the way to walk them. The markers answer "is there a comment here?"; they cannot answer "what did anyone say about this paper?", because the answer is spread over forty sheets and each marker holds its text behind a click. So this is the *reading* surface: a row carries the whole remark, wrapped and never clipped, and the page is where the remark sits rather than where it is read. Three decisions. **Next/Previous walk a ring** (`stepNote`), in the document's own reading order (`placedNotes` — sheet, then down the page, then across it, never the annotation array's order, which is whatever the producer happened to write), so reaching the last one and pressing Next again brings you back to the first — which is how a reader checks they have been through all of them. **A row goes to the remark; it does not open it** — the text is already here, and opening the card as well would put a focused textarea over the page for somebody who only wanted to see where the comment was; ✎ is the separate, deliberate act. And **autosave is stated, not assumed**: the switch lives here, where remarks are worked on, and the line under it says what the guarantee is worth — remarks are written on their own, a page move or a blackout never along with them. Wears the contents sidebar's chrome, because both are lists of what is in the document, read beside it. |
| `embed/pdf/PdfTextLayer.tsx` | **Selectable text over a rendered page** (#pdf-textselect). The reader paints to a canvas and a canvas has no text in it, so until this existed the only way to get a sentence out of a PDF was to retype it or to copy the *region* as an image (the ✂ tool, a picture of the words rather than the words). It is pdf.js's own `TextLayer` — one transparent, correctly-placed span per run — so the browser's ordinary selection and Ctrl+C do the work. Three things about it. It is **not a mode**: it was one briefly, and that was wrong — selecting words is what a pointer over text does everywhere else, and a reader who has to arm a tool first will retype the sentence. What made it a mode is that the layer takes the pointer over the whole sheet; the answer is pdf.js's own viewer's, which is to stack everything that needs its own click (links, search hits, markers, highlights, the blackout and region-copy surfaces) **above** it by z-index and leave the plain drag to the text. That stack lives in `themes.css` and is load-bearing — drop a `z-index` and that layer silently stops being clickable. Ctrl-click still reverse-searches, from the page wrapper where every layer's click bubbles to. **Only near pages build one**, the canvas render's gate and for its reason, which is also what makes always-on affordable: one text read per page actually scrolled to, the cost Ctrl+F already pays. And **zoom costs nothing**: pdf.js lays the spans out in the page's own units and multiplies by `--scale-factor` in CSS, the bargain the link boxes and the search hits already strike by storing big points. The `.textLayer` CSS is a scoped copy of `pdfjs-dist/web/pdf_viewer.css`'s rules rather than an import of that file, which also styles a whole viewer application we do not use. |
| `embed/pdf/PdfSelectionBar.tsx` | The bar that appears over selected text (#pdf-textselect): four highlighter colours, "highlight and write a remark", and the copy chip. It exists because selecting a sentence is almost never the end of what the reader wanted, and the two things they wanted next have nowhere else to live — a toolbar button is a trip away from the sentence and has forgotten which one was selected by the time it is pressed. Four decisions. A **colour swatch *is* the highlight button**: there is no "Highlight" that then asks for a colour, because marking a sentence is a mid-reading act and a two-step one is a step too many; four colours rather than a picker, for the reason a highlighter pen comes in a handful. The **remark is a separate button**, not a modifier, since it produces a card with the caret in it over the page and the plain mark must not. The **copy chip says what already happened and undoes the habit, not the copy** — a clipboard write cannot be taken back, so the honest control is the next one. And it **never covers what was selected**: it hangs above the last line and flips below at the top of a page, the same rule the remark card flips side in the margin by. |
| `embed/pdf/selection.ts` | What the reader has selected, as geometry (#pdf-textselect) — the translation from a DOM `Range`'s client rectangles back into big points from a page's top-left, the space every other overlay in the viewer lives in. Three decisions. **A selection is read per page, not per document**: a drag across a page break is one selection to the engine and one annotation per sheet to a PDF, so the rects are sorted into the page wrappers they land in and clipped, which makes the cross-page case fall out of the same code as the ordinary one rather than being a case nobody tested. **Line rects are merged, and only along a line** — `getClientRects()` gives one rectangle per text-node fragment, so a sentence crossing a font change arrives as five boxes that would become five overlapping quads, visibly darker at every seam; merging *across* lines is refused, since that would paint the leading between them. And **nothing here reads the page's text content**: the words come from the selection the reader made, so what a highlight quotes and what a copy produces cannot disagree. Also the guard that keeps a selection inside a remark card — which is a child of the page wrapper it belongs to — from raising a bar offering to highlight the page underneath. Tested in `src/__tests__/PdfSelection.test.ts`. |
| `embed/pdf/PdfLinkDialog.tsx` | The confirm before a link in a PDF leaves the app — `MailMessageView`'s link confirm applied to the other place the app renders somebody else's document, wearing that dialog's chrome rather than a second look for the same question. Host called out, full address monospace and **never** ellipsis-truncated (a shortened URL is itself the attack), and no "always open links from PDFs": a switch whose only function would be to remove the dialog on the one click that needed it. |
| `common/PageStrip.tsx` | **The** page-thumbnail strip: drag-reorder, shift-select, turn, delete, right-click. Used twice — horizontally by the print preview, vertically as the PDF viewer's page rail. |
| `common/mountPageStrip.tsx` | `createRoot` adapter letting the imperative print modal host `PageStrip`. |
| `common/Dropdown.tsx`, `common/OrbitSpinner.tsx` | Shared primitives. |
| `common/ConnLamp.tsx` | Red/orange/green SSH/OpenVPN status lamp (dialog + header). |
| `common/TimeField.tsx` | The clock-entry field the **calendar's event dialog** uses (an event's start and end), drawn by Eldrun rather than by the engine. The board card's due time is **not** this any more: two 2ch segments read as broken chrome beside a date input and gave no visible feedback while typing, so that field is now a pair of `00`–`23` / `00`–`59` `<select>`s (`todo/TodoCardDialog.tsx`) — always 24-hour, nothing to mistype, same `"HH:MM"` on the wire. The native `<input type="time">` was tried in between and is the one option ruled out for good: its 12-vs-24-hour face comes from the engine locale, so it printed `05:30 PM` under a 24-hour setting. It replaces `<input type="time">`, which cannot be told which clock to show: WebKitGTK reads that face off the **process** locale and ignores the element's `lang` outright (probed — `en-GB`, `en-US` and untagged fields all render `05:30 PM` under `LANG=en_US`, and all three flip together only when the process locale does), and Chromium follows the browser's UI locale, equally out of reach. So `Settings.time_format_24h` moved every clock the app *prints* and left the one control an hour is *entered* in behind: a card due at 17:00 read `17:00` on the board and `5:30 PM` the moment it was opened. It is **not** a text box — that was the first attempt and it was wrong: a free-text field accepts "soonish" as happily as an hour, and a deadline is not a place for a string somebody has to be told about afterwards. So it keeps the native widget's *shape* (two numeric segments, plus an AM/PM toggle on a 12-hour face) and its habits: digits only, two digits — or a first one too large to start a two-digit hour — advances to the minute, an overshooting second digit is reread as a fresh first one (`1` then `9` on a 12-hour face is 9), ↑/↓ spin the focused segment within its own range, ←/→ walk between them, `a`/`p` flip the meridiem. Everything it holds is a valid clock **by construction**, so there is no parse to fail; the only values it ever reports are `"HH:MM"` and `""` (both segments empty — a due date with no hour is the ordinary case), i.e. exactly what the native input handed its callers, so nothing above it changed. Advancing between segments is `focus()` and **never** `select()` — selecting text does not move focus, which is what broke the first cut of this: the caret stayed in the hour, so "1730" was read there as 17 → 73 → 3 → 30 → 0 and the field committed nothing at all (`TimeField.test.tsx` types straight through, with real focus, precisely to keep that regression caught). Two further rules are load-bearing: the stored value is re-read into the segments **only while nothing inside has focus** (otherwise `1` becomes `01` between the two keystrokes of `17`), and leaving the field settles a lone hour at the top of that hour rather than discarding it. Chrome is `.time-field*` in themes.css — the box wears the *caller's* `.cal-input`, so the border, height and focus ring stay the app's and the segments inside are bare. Tested in `src/__tests__/TimeField.test.tsx`. |
| `common/SyncConfirmDialog.tsx` | **The** confirmation every manual byte-sync transfer asks for, mounted once per window (`AppShell` + `DetachedApp`) because a pull or push can be started from the file tree, the file view's toolbar or the diverged-files list, in the main window or a popout — and none of them may carry its own copy of the question. It is `LocalLossDialog`'s opposite number: that one reports a loss after the fact, this one is the gate before it. What it says is what the old one-click buttons could not: a pull writes the host's bytes over the mirror's and a push writes the mirror's over the host's, so from the button alone the safe case (the receiving side holds nothing) and the lossy one (it holds edits held nowhere else) look identical — hence direction, scope, file count + size, how many land on top of an existing file, and **by name** the files whose content would be gone. The numbers are the backend's read-only `sync_transfer_preview`; a failed preview, or a tree too big to inspect up front, is stated and still requires the click, because a missing price is not an implicit yes. Untested. |
| `common/LocalLossDialog.tsx` | Warns that lockstep or sync **destroyed something in the local mirror** (#28q). Mounted at the shell, like the alarm popup: a background pass can delete a file while the user is three tabs away. It reports, it does not confirm — the write has already happened; the gates that prevent one live upstream. |
| `stats/StatsRecap.tsx` | Usage recap dialog: agents/models used, prompts asked, file churn, commits, time per project, Day/Week/Month. |
| `stats/StatsRecapHost.tsx` | Decides when the recap is on screen: once per day at launch (anchored on *yesterday*), or on demand via the `eldrun:open-stats` event. Mounted in `AppShell`. |

**Stores (`src/stores/`), hooks, lib**

| File | Purpose |
|------|---------|
| `projects.ts` | Project list, active project, CRUD, `setActive`. Also owns scope restore: `restoreProjectScope` (one project's saved tabs into its own scope, no switch) and `restoreActiveProjectScopes` (every **active** pill at launch — active means its terminals were never stopped, so they resume without waiting for a click). |
| `tabs.ts` | Tab/subwindow layout tree per scope; tab persistence policy. |
| `boxes.ts` | Project boxes: N:M membership (`boxMembership`/`useBoxMembership`; `addToBox`/`removeFromBox`/`boxProjects` — no silent dissolve), the persisted `box:<id>` scope's restore + seed (`restoreBoxScope`), and the box-scope helpers (`boxFolderOfScope`, `boxMembersOfScope`). |
| `settings.ts` | App settings (theme, default agent, git profile, shortcuts, etc.). |
| `ollamaAutoload.ts` | **Load a local (Ollama) model into memory at Eldrun start** (`settings.ollama_autoload_models`, armed per model by the 🧠 menu's "On start" chip and the Ollama settings list). An *installed* model is not a usable one — the first request pays the whole load — so anything meant to answer promptly and unattended (autocomplete, grammar, a future mail-importance check) needs one already resident, which until now meant clicking Load every launch. Three rules. **Energy Saver suppresses it** (a resident model pins GPU/CPU memory and is kept warm — the exact standing cost that mode exists to remove), with `settings.ollama_autoload_in_energy_saver` as the explicit opt-out, and the skip is **announced** rather than silent: the 🧠 button wears an amber `!` and its menu carries the notice + a "Load now" button, because a quiet skip is indistinguishable from a broken switch — but **only over what is actually missing** (`pending`): Ollama is a machine-wide server, so an armed model can already be resident (an earlier session left it warm, or something that isn't Eldrun loaded it), and announcing the skip over the whole armed list put "Not loaded at start (deepcoder)" directly above that model's own green *loaded* row in the same menu. The decision therefore reads what is resident first, names only the remainder, and shows nothing when there is none; `noteResident` (fed by the menu's model list and by a status flip to `loaded`) keeps it true afterwards, one-way — it only ever shrinks the set, since an eviction minutes later must not re-raise a launch-time notice. **A skip stays skipped until a click** — plugging in later does not start models minutes after the launch the user was told about. And the run is **sequential**, because two models loading at once contend for the same VRAM and can push each other back to CPU. `useOllamaAutoloadOnLaunch` (mounted in `AppShell`, main window only) waits for both `settings` *and* the power store's first reading — asking "is Energy Saver on?" before any battery state exists answers "no" for a laptop on battery. Tested in `src/__tests__/OllamaAutoload.test.ts`; untested live. |
| `windows.ts` | Embedded app windows. |
| `detached.ts` | Detached/popped-out subwindow state (#42). |
| `drag.ts` | Isolated per-frame drag state (reference for fine-grained selectors). |
| `activity.ts` | PTY-output activity outside React (`lastOutputByPty`). |
| `timer.ts` | Per-project time-tracking state. |
| `usage.ts` | Usage counters: in-memory accumulator (`bumpUsage`) flushed in batches to `usage_bump`, + the recap's read store. |
| `hpcPipeline.ts` | Open/closed state for the guided HPC pipeline wizard (`docs/quirky-knitting-umbrella` Phase B) — one boolean + open/close, so the **+** menu launches `HpcPipelineWizardHost` (mounted in `AppShell`) without prop-drilling. Mirrors `remoteMachines`. |
| `hpcJobs.ts` | SLURM jobs this session submitted, per project (`docs/quirky-knitting-umbrella` plan) — so the Jobs view shows a just-submitted job (and can re-open its log) before the next `squeue` poll, and can resolve a job id back to its out-file. `squeue` on the host is the source of truth; this is only the small in-memory memory of "what did I launch, and where's its log" (no persistence — out-paths are re-derivable via `scontrol`). |
| `caldav.ts` | CalDAV accounts and the sync that keeps their calendars current — a **second** store beside `calendar.ts`, mirroring the backend's own split: an account carries a login, a per-collection cursor and a keychain reference, none of which is calendar-display state. What a sync produces lands in the calendar store, where every existing surface already reads it; this one never renders an event. Two rules it lives by: a sync is **not** a replace (it goes through `caldav_apply`'s merge, so a card keeps the board column the user dragged it into), and a failure is **visible** (`status` per collection, because an account whose password was never saved fails every unattended sync after a relaunch and a quietly stale calendar looks exactly like one with nothing new in it). Subscribing stores **no** ctag or sync-token — a cursor written before the first fetch would make that fetch skip the whole calendar it was meant to import. Since Phase 3 it also owns **push**: `pushRow` (the gate — no account, no opt-in, or a server-side read-only collection all mean `skipped`, and nothing reaches the network), the `conflicts` list a `412` lands in, and the three answers to one. Two asymmetries are load-bearing. A failed **upsert** is remembered and *not* rolled back (it is already written locally; an unreachable server is a reason to report, not to undo the user's edit), while a failed or refused **delete** rejects, which is what stops the local delete — `CALDAV_CONFLICT_ERROR`/`isCalDavConflict` let a caller tell "already explained by the dialog" from "say something". And `applyWritability` re-derives every linked `Calendar.readonly` from both gates whenever either can move, because a calendar that accepts edits while never sending them is the failure the whole feature is built to avoid. |
| `calendar.ts` | Global calendars/events/tasks store (one `calendar.json` across all scopes) — and, since the to-do board, the **only** owner of task persistence for both surfaces: `moveTasks` (a whole drag in one backend write, so a concurrent edit cannot land between N per-card writes) and `setColumns` live here rather than in `stores/todo`, because two stores writing one file is two writers for one file. |
| `todo.ts` | The to-do board's **session** state and nothing that is written to disk: the overlay flag, the filters (deliberately not persisted — a filter that survives a relaunch is how a user loses cards), the in-flight card drag, the optimistic placement overlay, the urgent-mail cache, `collapsedSteps` (the cards whose checklist the user folded — a set of *exceptions*, since a card with steps now shows them; in the store because the card unmounts with the overlay and a fold that undid itself on the next open would be a flicker rather than a control), and `focusTaskId` — the header list's "open **this** card" request, a one-shot consumed and cleared by `TodoPane` (a kept selection would re-raise a closed dialog the next time the board opened, and the pane mounts a frame after the flag flips, so there is nobody to hand it to at call time). The drag lives here and **not** in `stores/drag`: that store's `drag !== null` is read by `CenterPanel`/`DetachedCenterPanel`/`FileTree` as "a tab or file drag is in flight", so populating it would put the center panel into drag mode behind a modal. |
| `browser.ts` | The in-app browser's store (#61): per-tab reader state, plus the machine-global capabilities / live windows / the one download awaiting a decision. Inherits the mail store's three rules, which matter more here: **nothing reaches the network on its own** (mounting registers a tab and reads capabilities; `load` and `openLive` are the only outbound paths, and both are clicks), **every action tolerates a rejected invoke** (the backend may be missing entirely), and **a download is refused by default**. Deliberately has **no** view-suppression refcount and **no** live-view LRU — Plan A specified both for a native child webview that Plan C proved cannot exist. The gate's **three** outcomes ride two wire fields, and the middle one is the load-bearing part: `allowed: true` *with* a `reason` is a loopback/private/link-local address, which is parked as a `PendingConfirm` and fetches nothing until a click answers it. The consent is per tab, in memory, forgotten with the tab (`approved`), and the live-page path goes through the same question so it cannot be the way around it. `browser:blocked` is routed by its `window_label` into `liveBlocked` — every emitter is a live *window*, so folding one into a reader tab would replace a page that tab loaded successfully; `lastActiveKey` is only the fallback for a future emitter that names none. |
| `mail.ts` | Global mail store (accounts, folders, the header page, selection, sync state). Like the calendar store it is one set across every scope — which is exactly what retired the mail tab, since a scoped tab over a global store could only duplicate the header overlay. Unlike the calendar store, **every action tolerates a rejected invoke** by landing in `error` rather than throwing, and nothing here reaches a server except `checkMail`. Also owns the list's **order** (`sort`/`sortDesc` → `setSort`, sent to `mail_headers` rather than applied to the page): the order belongs to the folder, not the page, so a re-sort re-reads page 1 and deliberately drops the offset — row 200 of a date-sorted folder is not row 200 of a size-sorted one. Also owns the **Important/Urgent** lists: `selectedPriority` and `selectedFolderId` are the two mutually-exclusive states of ONE list, and `loadPage` is the single fork between `mail_headers` and `mail_priority_page` — deliberately there rather than in the pane, so the pager, the search box, the sort menu and `MailList` never learn which they are showing. Modeling a priority list as a pseudo-*folder id* was the obvious alternative and is wrong: a folder id is what `mail_mark_folder_read` and `mail_move` are addressed by, so a leftover one is a command aimed at a folder the user is not looking at — hence selecting either clears the other. `openAccountView`/`openPriorityView` are the header dropdown's entries — "open the overlay **on** this" as ONE action, because a caller doing it in two would either render a frame of the previous target or retarget an overlay that is not open yet. Tested in `src/__tests__/MailPriority.test.ts`. |
| `skills.ts` | One boolean: is the **Skills Library overlay** on screen (`docs/skills_plan.md`). A store for the ordinary reason the other overlays use one — the control that opens it is in the header's 🧠 menu and the overlay is mounted at the shell. Deliberately holds nothing else: the sources, the catalog and the installed lists are `SkillsLibraryView`'s own reads and are the same reads the tab makes, so a store-held *copy* of them would be a second answer to "what is installed" that could disagree with the disk — exactly what the feature's no-manifest rule exists to prevent. |
| `alarms.ts` | Reminder ticker: fires an OS notification + the in-app popup, exactly once each. |
| `linkRouting.ts` | Routing of clicked links/URIs to viewers or external apps. |
| `pdfSync.ts` | Bidirectional PDF/SyncTeX sync state. |
| `pdfDrag.ts` | Dragging PDF pages between page strips, incl. across two Eldrun windows: strip registry + the `PDF_DRAG_*`/`PDF_DROP_ACK` protocol (OS-cursor stream in physical desktop px; bytes ride the backend `pdf_clip` slot, never the event payload). Also home to the **drag-active signal** behind the spring-loaded rail (`subscribePageDragActive`/`setPageDragActive`): the rail is the only drop target, and it starts closed, so a viewer holding it shut is not merely an unlikely destination but an impossible one — no mounted strip means nothing in `strips` and, the foreign listener being refcounted by mounted strips, nobody listening either. The signal is deliberately **not** derived from the `PDF_DRAG_*` events alone: a drop landing in the window it started in is resolved straight from the DOM and emits no END, so rails opened for it would never be told to close — hence `PageStrip` reports the drag's lifetime directly and the events only carry it to the *other* windows. Its own window-level listener is refcounted separately, because its subscribers (viewers) outlive the strips. The feature also forced `listenForeignPageDrags` to snapshot its window frame **lazily off the cursor stream** rather than only on START: a sprung rail mounts mid-drag and never hears the start, and without a late snapshot it maps no coordinate and silently refuses every drop — the pages would bounce off the very rail that opened for them. |
| `editorJump.ts` | Cross-pane jump-to-location requests. |
| `vpnPrompt.ts` | State backing `VpnPasswordPrompt`. |
| `remoteStatus.ts` | Live SSH/VPN connection state for the header lamps. `byProject` holds the PRIMARY host (unchanged — the ~300 file/git readers only ever want the primary); **multi-host** worker hosts (`docs/multi_host_remote_plan.md`) live in `byHost[projectId][hostId]`, read via `sshOf`/`vpnOf`/`hostStateOf` which route `"primary"` to `byProject`. `setSsh`/`setVpn` take an optional `hostId`. |
| `hostSessions.ts` | **The** project's persistent (tmux) session list (TODO #85) — one reading, one poll, shared by every surface that shows it. It exists because `ProjectFilesView` is rendered many times over at once (the right panel, each Files (Project) tab, each subwindow's docked file column, in the main window *and* every popout), and the list used to be per-instance `useState` + a per-instance 7 s `tmux ls`: N SSH round trips per host per tick, and N answers that could disagree — a session killed in the docked column sat on in the right panel until *its* interval came round. Sessions are a property of the **host**, not of the surface looking at one. So subscribers are refcounted (`useHostSessions(projectId, enabled)`; `enabled` is the old per-instance `active` gate, so a background tab still costs nothing), the interval and the connectivity subscription live only while ≥1 surface is showing, and `dropRow`/`renameRow` land a kill or rename in every surface in the same frame. `showAll` is shared for the same reason *and* a mechanical one: it changes what the backend returns (project-scoped vs. the host's whole listing), so a per-surface flag would need a second poll per value. The last reading is deliberately kept when the last subscriber leaves — an empty Sessions view doesn't read as "not looked at yet", it reads as "nothing is running". Deliberately **not** folded into `hostBusy`, which reads the same `tmux ls`: that one is machine-wide and keyed by SSH target (it answers "is this machine working?" for a lamp shared with the header and every pill), so feeding a project-filtered count into it would make a busy host look idle. |
| `syncConfirm.ts` | Mediates that dialog: `confirmSyncTransfer(req)` resolves `true` only after an explicit yes (never rejects), the same bargain `hpcGuardPrompt`/`hostKeyPrompt` strike, so a call site reads as one line before the transfer. It fires `sync_transfer_preview` in the background *after* raising the dialog — the gate is the click, the preview is only what the click is informed by — and drops a preview that lands after the user already answered. A second ask while one is open is answered "no" rather than stacking modals or swapping the question out from under the reader. Nothing is remembered between prompts: a confirmation that could be worn down by a "don't ask again" would not be a gate. |
| `localLoss.ts` | The active project's local-loss log (#28q). Pulled from the backend's on-disk record — not pushed as an event — so a deletion during a background pass, or while the app was closed, still surfaces. |
| `presentation.ts` | Two counters that have to be readable **outside** the component owning them, because three unrelated `window` keydown listeners can see one Escape and none can see the others: `armed` (a `PresentationOverlay` has the marker/laser out — read imperatively by `DeckPresenter`, which would otherwise read Escape as "end the talk" when all the speaker meant was to holster the laser) and `presenting` (a deck presenter is on screen — subscribed by `FileViewerPane`, which stops rendering its own overlay so its Escape listener stops competing). Counters, not booleans: a popout and the main window can each hold one. |
| `vpnStatus.ts` | **Machine-level** OpenVPN state, keyed by config path: the tunnel reroutes the whole OS, so it outlives the project view and is shared between projects. Owns the holder refcount (`releaseVpn`) and the `markVpn*` helpers. |
| `hooks/useKeyboard.ts` | Global keyboard-shortcut hook. |
| `lib/shortcuts.ts` | Shortcut definitions, chord parsing/resolution. |
| `lib/codexHooks.ts` | Codex hook-trust state + the one-click "open Codex on `/hooks`" fix. |
| `lib/vpnConnect.ts` | The silent-connect gate: ask `vpn_can_connect_silently` **before** connecting, because `pkexec` prompts the user before OpenVPN validates anything — a doomed attempt costs a polkit dialog, and the modal after it costs a second. Store-free, so `stores/projects` can use it. |
| `lib/vpnAutoConnect.ts` | "Connect on launch", armed per `.ovpn` (`settings.vpn_auto_connect`): the machine-level twin of a project's `remote.auto_connect`, and like it, never prompts — re-checked at launch, so a stale opt-in leaves the tunnel down. Also home to `openVpnLoginInTerminal` + `pollVpnUp`: **the** non-headless VPN login (arm the tunnel, hand the connect command to a root-terminal tab, poll `openvpn_status` to a terminal mark), shared by the paths that are always non-headless *and* by the headless surfaces falling back to it for one connect (`stores/vpnPrompt`'s `useTerminal`). |
| `lib/i18n.ts` | **The** internationalization module — the ONE place every UI language lives (`en`/`de`/`es`/`fr`/`it`), so adding a string or a language is a single edit here, never a hunt through the tree. Dependency-free: a flat `lang → (key → text)` map with **English as the source of truth** (it holds every key; other languages fall back to English then the raw key, so a half-translated language degrades gracefully). The live language is a tiny zustand store (`useI18nStore`) seeded from a `localStorage` cache (first-paint before settings load) and driven by `applyLanguage`; **switching is instant** — `useT()` subscribers re-render in place, no reload. Persistence rides `Settings.language` (`stores/settings` `setLanguage` + `applyLanguage` on load; broadcast to popouts via `LANGUAGE_CHANGED_EVENT`, mirroring the theme path). Wired app-wide (~3700 keys) — every component, plus the contextual hints/guided tour/lessons content in `hints.ts`/`tour.ts`/`lessons.ts`. Tested in `src/__tests__/i18n.test.ts`. |
| `lib/machineSync.ts` | Keeps a **global machine** (header "Machines") and the **project host** it also is (a project's primary `remote`, or a `compute_hosts` worker) in step. The two are never linked by id — dropping a machine onto a project copies it by *value* — so the only bridge is the **SSH target** (`user@host:port`), which `sameTarget` owns (host case-insensitive, default port 22, empty user ≡ absent; a different login on the same host is a *different* connection). The physical connection genuinely is shared (one ControlMaster per target), so this mostly reconciles two status stores rather than opening a second socket. Two asymmetric rules keep it from looping or lying. **Global → project** rides a *subscription* on the machine's `status` → `connected`, so it fires whichever action connected the machine rather than the two that used to call it by hand (a machine turned green by a probe never reached its project at all — the "machine green, project disconnected" bug). It can be a subscription now because `status` no longer answers "reachable": `probeAll` writes `reachable`, and only a session this app opened writes `status`. For the **active** project it opens the real pool (`remote_connect`); for a loaded-but-inactive one it writes **nothing** — a mirrored green lamp both lied (no pooled session behind it) and *blocked* the later real connect, since activation only proceeds from `off`. Skips a host tagged HPC before opening a pool: `remote_connect` raises a `ControlPersist=600` master, the standing presence the tag forbids. (Disconnect stays imperative — it has to mirror the teardown *before* `remote_kill_all_jobs`, an ordering a subscriber cannot reconstruct.) **Project → global** is a *subscription* (`initMachineSync`, installed once in `AppShell`) that only ever **upgrades** a matching machine to `connected` — `off` is deliberately never propagated upward, because a project deactivating *clears* its lamps and would otherwise knock an independently-connected machine offline. No loop is possible: the global store's `setStatus` never writes `remoteStatus`, so the subscription can't re-enter the imperative path. |
| `lib/viewers/{fileUtils,markdown,highlight,tex}.ts` | Pure viewer logic (XSS-safe markdown/highlight, TeX, file utils). `tex.ts` also owns the two halves of a **cross-reference**: `findTexKeyRefAt`/`texKeyRefRanges`/`resolveTexKeyRef` (#tex-ref-jump) make a `\ref{…}`/`\cite{…}` a Ctrl-clickable link to the `\label{…}` or the `.bib` record that defines it — the question `\input`-following (#49) and the key dropdown (#cite-ref-complete) left unanswered, since the key is typed here and defined somewhere in a 40-file document. A key reference resolves to a **position**, not a file, so it goes through the editor-jump channel and opens nothing when the definition is in the file already on screen; a key nothing defines is left alone, because a `\ref` written before its label is an ordinary state of a draft rather than an error to report at a click. The document walk both this and the completion list run on is **one** function (`walkTexSources`) — a dropdown offering a key the click cannot reach is what two copies of it would eventually produce — and the file being edited is searched from the caller's *draft*, so a label typed a minute ago is findable. Tested in `src/__tests__/TexRefJump.test.ts`. `tex.ts` is also where the PDF's **Ctrl+F** lives (`pdfPageMatches`), and its one subtlety is that a PDF has no words and no lines — only positioned runs of glyphs — so a wrapped paragraph arrives as `"hyphen-"` then `"ation"`. Joining the runs end to end (what it used to do) produced `hyphen-ation`, i.e. *hyphenation* could not be found on a page that plainly prints it, and two words either side of a break joined into `theend`. `pageHaystack` therefore **reads the breaks**: a trailing hyphen at a line end is dropped (never an en or em dash, which there is punctuation — `pages 3–\n4` must not become `34`), any other break becomes a space unless one side already carries whitespace, and pdf.js's own `hasEOL` is trusted over the geometry (`TextItemBox.eol`, which is also why an empty run — pdf.js's bare end-of-line marker — is folded into the run before it rather than skipped). A per-character map back to (run, offset) is what keeps the highlight honest through all of that: boxes are still sliced out of the runs' own geometry, one per line, and a match running *through* a dropped hyphen covers it rather than stopping a glyph short. Case folding happens per character for the same reason — `İ` folds to two characters, and lowercasing the joined string would shift every index after it and slide the boxes off the words. |
| `lib/viewers/python.ts` | Python editor intelligence (#87), all pure — every fs touch is injected. Two halves: **breakpoints** (pdb refuses blank/comment lines, so a gutter click *snaps* to the next executable one; a line number re-points at the wrong statement when you type above it, so every edit *remaps* them) and **go-to-definition** (a *lexical* import-graph walk — relative levels, aliases, `__init__` re-exports, src-layout — not a type inferencer). Only names it can actually follow are underlined, so the ctrl+click affordance never lies. |
| `lib/browser.ts` | **The** typed invoke surface for the in-app browser — one wrapper per `browser_*` command, and no component invokes directly (asserted by `BrowserTripwire.test.ts`, which walks all of `src/`). **No wrapper takes a filesystem path**, because no command does: downloads cross the boundary only through `browser_download_decide`, where the backend raises the OS save dialog. `READER_FRAME_CSP` is `MAIL_FRAME_CSP` *imported*, not copied — two policy strings that are meant to be identical will drift, one that is the same object cannot — and `readerLooksUnsafe` is mail's `bodyLooksUnsafe`, re-exported, for the same reason. `browserCapabilities()` resolves rather than rejects when the command is missing, so a frontend built ahead of its backend still lands in a definite "nothing is supported" state. Also home to `REASON_KEYS` / `reasonPhrase` / `errorPhrase`: the backend speaks in machine tokens (`app-origin`, `scheme:file`, `redirect-to-link-local`) so the wording can live in `i18n.ts` ×5 — the price is that an untranslated token reaches the user *as a token*, so `services::web_safety::REASON_TOKENS` is the canonical list and `BrowserTripwire.test.ts` reads it out of the Rust source and fails if either side moves alone. **Origin emphasis is the whole host, not eTLD+1** — a documented, tested v1 limitation: a two-label guess bolds `co.uk` for `shop.example.co.uk`, and a real PSL would change `registrable()`, which is mail's and is cached under `SANITIZER_VERSION`. |
| `lib/linkTarget.ts` | The **pure** URI routing table (#33) + the address bar's commit rule, plus the dispatcher that performs a decision. Two load-bearing rows: a URL **Eldrun itself** started is always external (an OAuth flow routed into a fresh profile just means logging in again in the wrong place), and a URL that arrived from **untrusted content** — a mail body, terminal output, an agent's answer, a viewed file — opens in reader mode and may not become a live page in one click. An explicit gesture beats the setting in both directions. `parseAddressInput` never navigates a non-`http(s)` scheme and reads `localhost:5173` as a host and port rather than a scheme. `joinConference` is the one call site that names its own target: a video call is routed `explicit: "external"`, because a conference URL is a door rather than a page — the session, the camera permission and the "open in the desktop app" handoff all live in the user's real browser, and an inert sanitized copy of Zoom's launch page is a Join button that does not join. It still goes *through* the router, so the scheme gate applies to it like everything else. Tested exhaustively in `LinkTarget.test.ts`. |
| `lib/mail.ts` | **The** typed invoke surface for the mail client — one wrapper per `mail_*` command, and no component calls `invoke("mail_*")` directly. **No wrapper takes a filesystem path**, because no command does: that is the whole statement of the sandbox boundary. Also home to the pure render pieces — `buildMessageSrcdoc` (the frame document, with `MAIL_FRAME_CSP` frozen as one constant string) and `bodyLooksUnsafe`, the tripwire that refuses to render a body the backend's sanitizer should already have made inert (a regression detector, deliberately not a second sanitizer). Plus the **`Authentication-Results` display rules** (`mailAuthShown`/`mailAuthTone`/`mailAuthSummary`, tested in `MailAuthDisplay.test.ts`): `mailAuthShown` returns nothing outside the backend's `verified` state — a second refusal on top of `mail_authres`'s own, because the failure mode here is not showing too little but teaching the user to trust a tick an attacker drew — and `mailAuthTone` tones a `pass` as good **only when it is aligned**, since a signature by `evil.example` on a mail claiming to be a bank passes genuinely and means nothing. An unrecognized result token never inherits a pass's appearance. The `mailPriority*` trio is the only part of this surface that reaches **no network in either direction**, and that is the feature rather than an omission — see `types/mail.ts`'s `MailPriority`. |
| `lib/slurm.ts` | **SLURM run/watch glue for HPC projects** (`docs/quirky-knitting-umbrella` plan), modeled on `pythonRun.ts`. Two halves: (1) **pure text helpers** — `isSlurmScript` (has a `#SBATCH` line), `parseSbatchDirectives`/`directiveValue`/`spliceDirective` (the render-rows/edit-text bargain the YAML/table viewers use: the directive form edits the script's `#SBATCH` lines by **splicing**, never re-serializing, so comments/untouched lines/the flag spelling all survive and each edit is an ordinary dirty/undoable change; short flags fold into their long key); (2) **tab glue** — the **log window** (`tail -n +1 -F <out>`, follows-by-name so it waits out the queue) and the **interactive shell** (`srun --pty … bash -l`, lands on a compute node — the compliant path) are just terminal tabs, exactly as a Python run is, so they inherit host locality + tmux persistence for free. `submitSlurmJob` invokes `slurm_submit`, opens the log tab, and records the job in `stores/hpcJobs`. Passes the file's **absolute** path as the script id so one command works remote or local. Untested until cluster QA. |
| `lib/hpcWorkspace.ts` | **HPC workspaces** (`docs/quirky-knitting-umbrella` Phase C; backend `commands::hpc_ws`) — the step that must happen *before* any data reaches a cluster. `$HOME` there is a small quota'd filesystem for code; the bulk data belongs in a **workspace**: a directory on the parallel filesystem with a name, a duration and an expiry after which it is **deleted**. Invoke wrappers (`wsAvailable`/`wsList`/`wsAllocate`/`wsExtend`/`wsRelease`/`wsLink`) plus the pure helpers the two surfaces share — `projectPathIn` (where a project inside a workspace goes), `remainingLabel`/`expiryTone` (the clock, toned because expiry *deletes*), `defaultFilesystem`, and the `wsTargetFor{Project,Host}` pair that encodes the one asymmetry: a workspace is usually allocated **before the project exists**, so the command can also run against a bare, just-authenticated host. `linkedWorkspaceCaveat` is the single wording of "a symlinked workspace is reachable to the host's tools, not to Eldrun's byte-sync" (which is lstat-typed and skips symlinks by design). Also the **anchor + lifecycle** half (`docs/hpc_workspace_plan.md` Phases 1–2): `wsAnchor`/`setProjectHpc` (the small `$HOME` folder — `logs/`, a `workspace` link, an append-only record — plus the `ProjectEntry.hpc` record that is the ONLY thing naming the workspace once it is deleted), `defaultAnchorRel`/`logOutputPattern` (`eldrun/<name>` and the `--output` that puts a job's log where it outlives the workspace it ran in), `findProjectWorkspace` (**by recorded id first, then by path containment** — a plain `startsWith` would match `…-demo2` against `…-demo` and point Extend at the wrong allocation), `shouldWarnExpiry`, `moveProjectRoot` and `pullLogs`. Plus `scratchCandidates`/`freeSpaceLabel` for a cluster with **no** workspace tooling — the site's own `$SCRATCH`/`$WORK`/… offered as the project root, since otherwise such a project lands in `$HOME`, the failure the step exists to prevent. Site-agnostic throughout — the host is asked what it offers, and the only site strings anywhere in the feature are inside test fixtures. Tested in `src/__tests__/HpcWorkspace.test.ts`; untested against a real cluster. |
| `lib/skills.ts` | **The** typed invoke surface for the Skills Library (`docs/skills_plan.md`) — one wrapper per `skills_*` command, the convention `lib/mail.ts`/`lib/printing.ts` set. There is deliberately no manifest or install-state tracking on this side either: `listInstalledSkills` is a plain read of what is actually on disk under a target's `.claude/skills/`, matching the backend's own "the tree is the only source of truth" rule. The install trio is addressed by a `SkillTarget` and **never by a path**, and `PERSONAL_SKILLS` is a frozen constant rather than a function for that reason: there is one way to name the personal scope, it takes no argument, and no call site can turn it into a directory this side chose (`SkillsTarget.test.ts` asserts the shape that crosses the boundary, not just the command name). |
| `lib/localDrivers.ts` | **The** typed invoke surface for the local-model coding agents and the model-update check — the convention `lib/mail.ts`/`lib/printing.ts` set. `listLocalDrivers(model)` takes the **active model**, and that argument is the whole feature: every agent it lists (Codex, Claude Code, OpenCode, Droid, OpenClaw) drives its model through **tool calls**, so a completion-only Ollama model (`deepcoder`, `llama3`, `deepseek-r1`, …) cannot serve one at all — the server answers the first request with `does not support tools` and the tab dies on arrival, reporting a raw JSON error into a terminal nobody can act on. Not offering the combination is the fix, so `TabBar` and `NewTabMenu` both re-probe whenever the model changes rather than loading the list once. `needs_tools_unsupported` exists so an empty group can say **which** of the two reasons emptied it: withholding an agent that is visibly installed one group above reads as a bug unless the hint says the model is why. `checkOllamaUpdates` is **on demand only** — Ollama has no "is there an update" API, so a check is a manifest-digest comparison against the registry, i.e. the one outbound request the 🧠 menu makes, and it belongs to a button rather than a hover or a timer. A model the registry could not be reached for carries its own `error` and `update_available: false`, because "couldn't tell" must never render as an update badge the click cannot honour. `ollamaVersionStatus(checkRemote)` is the same question about the Ollama **server**, split on exactly that argument: `false` is a local `ollama --version` and touches no network, so the 🧠 menu shows what is installed on every open, while `true` rides the same "Check for updates" click. The two checks are settled independently (`Promise.allSettled`) because they reach different hosts — a registry that is down must not suppress the server answer. The notice's verdict is **dropped the moment the installed version changes**: that is precisely the case where the user just upgraded, and a banner still offering the upgrade they performed is the one thing it must not do. Upgrading goes through `runInstallInTab`, never a copy-this-command step — both installers need an interactive sudo/UAC answer, and a visible terminal is where it is given. `loadOllamaModel(model, device)` and `ollamaGpuStatus()` are the **is it even on the GPU** pair. `LoadDevice` is `auto`/`gpu`/`cpu` and three values rather than a bool, because deferring to Ollama's scheduler is a real answer and not the absence of one — which is also what keeps every existing call site (the autoload store, the settings list) meaning exactly what it did, since the default is `auto`. `LocalModelMenu` shows **GPU** and **CPU** buttons on every "Load into memory" row whenever `gpu_present`, and the single Load button only when the machine has no GPU at all: a GPU button on a machine with no GPU is a control that can only fail. The choice is asked **per load and stored nowhere** — which processor suits a model depends on the model and on what else is resident, so it is a question with a fresh answer each time rather than a setting. `ollamaGpuStatus` is read **once per open**, never on the memory-gauge poll beside it, because it spawns processes; the two questions look alike and are not (that poll asks how full the device is, this asks whether the device is being used, and only the second can be answered wrongly by a setting). A notice may be raised on `igpu_dropped` and on **no other field** — the backend requires four facts to line up before setting it, and this surface must not second-guess that by warning on `model_on_cpu` alone, which is equally what an ordinary out-of-VRAM looks like. Untested. |
| `lib/printing.ts` | **The** typed invoke surface for the print manager — one wrapper per `print_*` command, no component invoking directly (the convention `lib/mail.ts` set), plus the pure helpers the pane renders through. **No wrapper takes a filesystem path, because no command does**: printing a document you are looking at is already the viewers' job (`lib/viewers/print.ts` + the platform print dialog), so this surface manages queues only and can never be turned into a print-this-file path. `printSnapshot` **resolves rather than rejects** — a build whose backend predates the feature is in the same situation as a container with no CUPS, and should say so rather than throw. `printerStateKey`/`jobStateKey` narrow the backend's word to the closed set we have wording and a tone for, degrading an unknown one to `unknown` and never to `idle`; `printerTone` is where the two independent failures fold into one lamp (stopped **or** not accepting = bad). `orphanJobs` exists because the pane groups by printer: a job whose queue the snapshot no longer lists would otherwise vanish, and a job nobody can see is a job nobody can cancel. Tested in `src/__tests__/Printing.test.ts`. |
| `lib/pythonRun.ts` | Run/Debug a Python file (#87) by opening a **terminal tab** — not a bespoke exec path. That is what makes it inherit remote-host and container locality for free (the *tab* carries both) and stay a real interactive terminal (`input()`, `Ctrl+C`, output survives exit). Debug is pdb pre-loaded with the gutter's breakpoints (`-c "b f:N" … -c continue`). **Which** Python it runs is asked of the backend (`python_interpreter_for`), never re-derived here: two interpreter rankings that can disagree is a bug waiting to happen, and only the backend can see conda/poetry at all. |
| `lib/fastMode.ts` | **Fast mode** (`Settings.fast_mode`, Group U #210) — one switch that withdraws the display aids whose cost is a directory walk, a standing poll, or a read of every file in view. **The list of what goes lives here**, in the module header, rather than being spelled out at each call site, so the settings help text and the code cannot drift apart: folder sizes in the file tree (one `dir_size_breakdown` per visible folder — a `du` over SSH on a remote project — plus the group totals summed from them), the git-dirty dots on the project pills (a `git status` per local project every 12 s, for projects the user is not in), the project hover card (a `project_cpu_percent` poll every 1.5 s while the pointer rests) and the tab hover card (its own ticking clock per hover), the header CPU/RAM/GPU readout, the Python ▶ gate's `__main__` scan (an SFTP round trip per file on a remote listing), the tree's 15 s remote re-stat, and every animation and transition (`data-fast-mode` on the root, see `themes.css`). Nothing may be added that misses one of three properties: it costs work nobody asked for, its **absence is legible** (no spinner, no `…` that never resolves — a missing figure must not read as a stuck one), and nothing is lost but the aid. Fast mode may make Eldrun say less; it may never make it say something untrue. Deliberately **not** a fourth value of `energy_saver` (`stores/power`), which it composes with instead: that one widens timers off a live *battery* reading, this removes features off a standing *preference*, and one merged control could not express "plugged in, still want it lean". `useFastMode` is the reactive read (so a withdrawn surface comes back the moment it is switched off — no relaunch, no remount), `fastModeActive` the snapshot for effects and event handlers, and `applyFastModeAttribute` the per-window root publication, called from `AppShell` and `DetachedApp` the way the energy-saver attribute is. **Unset is off and `false` is off**: the mode is never inferred, unlike `time_format_24h` where unset genuinely means "derive it". Tested in `src/__tests__/FastMode.test.tsx`; untested live. |
| `lib/experimental.ts` | The experimental-flag gate: **off for everyone, on in debug mode** (unset ≠ false — it falls back to `settings.debug`, so a flag still moving is invisible to someone *using* Eldrun and present by default for someone *building* it). Read a flag via `useExperimental`, never `settings.<flag> ?? false` — that spelling is what makes a flag miss the debug default. Gates `agent_mode_toggle`, `python_run_debug`, `deck_presenter`, `mail_client`, `web_browser` and `terminal_webgl`. Also home to `EXPERIMENTAL_TAB_KINDS`/`withdrawnTabKinds`: the flags that own a whole **tab** — now just `web_browser`, for which "off" means the tab is *gone*, not merely absent from the new-tab menu. `mail_client` was the second and is not any more: mail lost its tab, so the flag gates a header button and an overlay, i.e. an entry point like any other. A layout saved while the mail tab still existed is handled by `stores/tabs`' `RETIRED_TAB_CMDS` instead — deliberately *not* here, because that filter must not wait for settings to load. `withdrawnTabKinds` returns nothing while `settings` is null, and that is the load-bearing part — unknown is not off, or the async settings read would close a restored mail tab in the gap before it lands. |
| `lib/experimentalSweep.ts` | The withdrawal itself: when a tab-owning experiment is switched off, close its open tabs in **every** loaded scope (`stores/tabs`' `closeTabsOfKinds`) and any live browser window. Installed once per window in `AppShell` and re-run on every settings change, because a flag can go off without the Settings panel being open (Debug mode carries every unset flag with it, and settings arrive late at launch). Two surfaces it deliberately does not own: a **popout** sweeps its own tabs (separate React root, separate store — `DetachedApp` streams the ordinary `close` edit back), and a **restored** tab is dropped by `loadFromLayout`'s filter rather than closed after the fact, so the sweep and the restore never race. |
| `lib/customScrollbar.ts` | **The app's own scrollbars**, because the engine's cannot be squared. WebKitGTK — every Linux build — never implemented the `::-webkit-scrollbar` pseudo-elements; the one mechanism it honors is `scrollbar-width`/`scrollbar-color`, which expose thickness and colour and **no corner radius**, so the thumb is whatever GTK draws (a capsule). Dropping the standard properties to get the webkit path back is worse — the engine then falls back to the native light GTK bar, white trough and all, which is exactly what the global baseline in `themes.css` exists to prevent. So the native bar is hidden and a thumb is painted instead, on **every** platform, rather than letting Linux drift. Installed from `bootstrap.tsx` and therefore in every window (main shell, popout, presenter audience), each of which needs its own layer; a DOM-level concern with no React state, so it is imperative rather than a component. Four things carry it. Discovery is **automatic and opt-out**: a container qualifies by scrolling, and declines by already computing `scrollbar-width: none`, which is how the app has always said "this strip scrolls but shows no bar" (the tab strip, the pill row) — a decision read out of the stylesheet where it was made rather than duplicated as a selector list here. The thumbs live in **one fixed layer**, never a wrapper around the container, because wrapping arbitrary scroll containers is what breaks React reconciliation. Whether a thumb is drawn at all is a **hit test** (`document.elementFromPoint` inside the container's content, clear of the gutter), not a list of which selectors count as a modal: one layer for the window means the right panel's thumb would otherwise paint over an open dialog, and asking the document what is topmost handles every overlay the app grows later. And the two update paths are **deliberately asymmetric** — a scroll reads three numbers off the element and writes one transform, with no `getBoundingClientRect` anywhere (an element's own scroll cannot move its own box, so the cached rect stays true, and `Entry.nested` is what limits a re-measure to the ancestors that genuinely stale one); a geometry pass re-measures, re-hit-tests and prunes, and runs only on resize, mutation or an ancestor scrolling. Both coalesce into one rAF and neither runs while the document is hidden. The native bar is suppressed **inline**, not by the `[data-eldrun-scrollbar]` rule alone — several surfaces name themselves in a later `scrollbar-width: thin` rule of equal specificity, which painted both bars at once. Geometry is pure and tested (`src/__tests__/CustomScrollbar.test.ts`); the DOM half needs a real engine, since jsdom lays nothing out. |
| `lib/tmuxSession.ts` | Persistent remote sessions (TODO #85), the frontend mint + decision helpers. `newTmuxSessionName(scope, kind)` mints the stable `eldrun-<scope>--<kind>-<uuid>` a shell/agent tab persists as `TabEntry.tmuxSession` (minted once at creation — the tab's PTY id is *not* stable, `loadFromLayout` regenerates the key, so a derived name would fork a second session on relaunch instead of reattaching). The `<kind>` token (`agent`/`shell`) rides at the front of the uuid half so it never touches the `eldrun-<scope>--` prefix the Sessions view filters projects by; `sessionKindFromName` is its pure inverse and is what the Sessions view groups a machine's rows by (Agents / Shells / Other), with `Other` catching any name that carries no token (foreign, hand-renamed, or minted before the token existed). Classification is name-only — the backend `TmuxSession` grew no kind field. `persistSessionsEnabled` encodes **default ON** (`persist_sessions !== false`); `shouldPersistTab` = a **shell** tab (Python runs open one) on a **remote host** of a persist-enabled project; `shouldPersistLocalTab` = a shell tab running **locally** (a local project, or a remote project's mirror tab) with `persist_local_sessions` on (Unix) — for surviving an Eldrun crash. Never agents, never root. The actual tmux wrap is the backend's; this only mints the name + picks *which* tabs get it. |
| `lib/hostBound.ts` | The **host-bound marker** a local-model tab needs to keep running on the host when its project's container toggle is on (`docs/sandbox_hardening_plan.md` Phase 2 / #150). Mints a uid at tab creation, registers it with the backend (`register_host_bound_tab` → a file under `<state_dir>/sessions/<project>/host_bound/`), and the tab persists it as `TabEntry.hostBoundUid` — minted once and persisted for exactly `tmuxSession`'s reason: `loadFromLayout` re-mints every tab key *and* its PTY id, so nothing else on a tab survives a relaunch. It exists because that exemption used to be granted by the tab carrying `ELDRUN_LOCAL_MODEL`, which `TabBar`/`NewTabMenu` set **for the usage recap** — an authority decision keyed on a telemetry label, so any later surface setting that var for a display reason would have handed out container escapes with nothing failing. The uid is only an *index*: the marker file is the grant, so a planted value buys nothing. Registration failing is not an error to handle loudly — the tab simply runs inside the container, which is the safe direction. |
| `lib/closeRemoteTab.ts` | The tab-close (detach) path for a persistent tab (TODO #85). Closing a tab is **non-destructive**: `closeTabWithConfirm` just `removeTab`s, which unmounts the pane and kills only the ssh/PTY *client*, so the tmux session lives on under its daemon and stays reattachable from the Sessions view. A clean Eldrun quit reaps only local Eldrun-owned (`eldrun-*`) sessions; an unexpected exit leaves those local sessions for restore. Remote tmux sessions are never part of that cleanup: they survive quit/disconnect/crash until explicitly killed in the Sessions view. A session's **×** (kill) in that view (`remote_tmux_kill`/`local_tmux_kill`) ends just that session. `persistentSessionOf`/`localPersistentSessionOf` classify which session a tab owns (the Sessions view marks owned rows). Reads the tabs/projects/settings stores, so it lives here — `stores/projects` already imports `stores/tabs`, so a store-level import back would be a cycle. |
| `lib/viewers/yaml.ts` | The YAML/JSON tree model + its edit ops, all pure. The tree is a **view on the text**: every edit is a surgical splice, never a re-serialization of the parsed model — the only way a config file's **comments** survive an edit, and what lets Tree and Source share one draft. **Both** YAML syntaxes are first-class: *block* (indentation) and *flow* (`{a: 1}` — i.e. **JSON**, inline or spread over lines). Which one a node is written in decides how it is edited (block → line splice, flow → span splice) and the tree keeps the author's choice, so `[a, b]` grows to `[a, b, c]` and is never quietly rewritten into block. `strict` (a `.json` file) is the only dialect knob: no plain scalars, so keys and strings are quoted. **Comments are a node's own, not scenery** — a `#` is the only place YAML documents a key, so each node reads the one behind its value *and* the run directly above it at its own indent, and `setComment` writes back wherever the author already wrote (a new one goes behind, which adds no line). The consequence is in the edits: a comment **travels with its node**, so deleting or reordering a key takes its description along rather than leaving it to re-attach itself to whichever key slid into that place. `moveNodeTo` is the one reorder op (the ↑/↓ buttons are `to = at ± 1`): a drag across five rows is **one** splice, not five swaps — every splice invalidates the offsets after it. A construct it could not rewrite without botching it (anchor, alias, merge key, plain scalar continued across lines) still *renders* but comes back `editable: false`; a line it can't classify at all fails the parse and defers to Source. |
| `lib/viewers/bib.ts` | The BibTeX/BibLaTeX model + its edit ops, all pure — and the ONE reading of the format in the app: `tex.ts`'s `parseBibEntries` (the `\cite` dropdown) is now a thin adapter over it, because the card view and the completion list must not be able to disagree about what is in a `.bib`. Two ideas. The cards are a **view on the text**: every op is a splice addressed by source offsets (`setBibFieldValue`/`setBibFieldName`/`setBibKey`/`setBibType`/`addBibField`/`deleteBibField`/`deleteBibRecord`/`addBibEntry`), never a re-serialization — the only way a hand-sorted, brace-protected, `%`-commented library survives an edit, the same bargain `yaml.ts` strikes for comments and `table.ts` for quoting. And the parse is **tolerant, and says what it could not take responsibility for**: it brace-matches each `@type{…}` (so an `@` inside a value is inert) and never throws, marks a value it cannot safely rewrite `editable: false` — a `#` concatenation or a bare `@string` macro reference, where turning `jan` into `{jan}` would change the rendered bibliography — and reports `strayLines` for bytes belonging to no record. A value's **delimiter is kept** (`{…}` stays braces, `"…"` stays quotes, a numeric bare stays bare) and promoted only when the new text can't live in it; an unbalanced brace is escaped rather than written through, since one would end the record early. `addBibEntry` mints its key by scanning for a free suffix, never by counting records — the `todoBoard` subtask-id rule, and here the failure it prevents is exactly what `duplicateBibKeys` exists to report. It also owns the **reading** half the card list is navigated by — `bibVenue`/`bibVenues`/`filterBibVenue` (the conference or journal, `bibMacros`-resolved: the one place a `@string` is expanded at all, and deliberately only for grouping, never for an edit) and `bibFirstAuthor`/`bibYear`/`sortBibRecords` — all of it display-only, never a reorder of the file: `sortBibRecords` copies rather than sorts in place, because the edit ops address the file by the offsets in the parse's own array. Its three rules are that an unkeyable record (no author, or an `@string`) sorts **last in both directions**, the file's order breaks every tie, and a name is *read* rather than parsed to BibTeX's name grammar (`van Beethoven` files under `Beethoven`) — an ordering off by one shelf beats a name parser that has to agree with the bibliography processor to be right at all. Tested in `src/__tests__/BibViewer.test.ts`. |
| `lib/viewers/yamlGrid.ts` | The YAML/JSON **card grid** helpers (#yaml-grid), all pure. `hasCards` is the toggle gate (true when a collection nests another); `isContainer`/`cellKind`/`nestedLabel` classify a node so the card view never offers a control it can't honour — every EDIT is still delegated back to `yaml.ts`'s splice ops (the view-on-text bargain: comments/quoting/untouched bytes survive). Also keeps the **tabular** model (`gridModelFor`/`gridCandidates`/`hasGrid`): the flat rows × columns view of a single list/map-of-records — the card view doesn't render it, but it stays a tested, reusable "is this region a table?" helper. Tested in `src/__tests__/YamlGrid.test.ts`. |
| `lib/viewers/table.ts` | The CSV/TSV model + its edit ops, all pure. Two things it exists to say. **The separator is guessed, not known** — `.csv` never states which character separates the columns, so assuming comma from the extension is exactly what renders a `;`-delimited export as one tall column; `sniffDelimiter` scores each candidate by parsing a sample *with* it (a comma inside a quoted field cannot fool the `;` reading) and asking how rectangular the result is, rejecting any character that never splits a row however consistently it fails to appear. And **the table is a view on the text**: `parseTable` hands back each cell's source span, so `replaceCell`/`insertRowAfter`/`deleteRow` are surgical splices — the only way editing one cell leaves every other field's quoting, the file's line endings and its delimiter byte-for-byte alone. Same bargain `yaml.ts` strikes for comments. |
| `lib/viewers/gif.ts` | The GIF decoder, all pure — takes a `Uint8Array`, never touches the fs (WebKitGTK has no `ImageDecoder` API, so frame control means decoding LZW + interlace + disposal 0–3 ourselves; GIF89a is frozen, so no maintenance tail). Two ideas: **every frame composites to a full-canvas RGBA buffer** (GIF has no keyframes — frame N is a patch on N−1's state, so upfront full frames are what make scrubbing O(1); memory is bounded by `maxPixelBytes`, beyond which the result says `truncated`), and **delays are stored as authored, played by convention** (`effectiveDelayMs`: <20ms means "browser default" = 100ms — files were authored against that clamp). Disposal 2 clears to *transparent*, not the spec's background color, because that is what every browser does. Corruption after ≥1 good frame returns the clean prefix + `truncated`; before it, throws. |
| `lib/viewers/pageModel.ts` | **The** page-arrangement model (`PageRef{id,src,page,rot,marks?}` list) shared by the print preview and the PDF page rail: move/delete/rotate/duplicate/insert, all pure. `marks` are the pending blackouts (#pdf-redact) and `notes` the pending remarks (#pdf-notes) — a remark being a sticky note at a point or, with `quads`, a highlight over the words those boxes cover; both ride on the *entry* rather than in a side map, which is what makes each free of its own machinery: they travel with a page that is moved, a duplicate gets its own copy, `isPristine` counts either as an edit so the file can be saved at all, and the viewer's existing undo/redo covers both. |
| `lib/viewers/pdfNotes.ts` | Remarks on a PDF (#pdf-notes and #pdf-textselect), the pure half — covering **both** kinds, since `isHighlight` (does it carry `quads`?) is the single place the distinction is made and everything else here is written once for the pair. `quadsAnchor` is where a highlight hangs from: the start of its *first line* by reading order, decided by the boxes overlapping vertically rather than by the walk's marker-sized line tolerance, which would read two lines of body text as one and anchor the card at the second line's start — `marks`' opposite number: a pending *addition* rather than a pending destruction, and one that is written into the file as the PDF's own annotation, so a remark made here opens as a comment in Acrobat, Okular or a browser and one made there opens here. Nothing is stored beside the document, which is the whole point — a comment only Eldrun can see may as well not have been written. The one rule everything else follows from: a sheet's remarks are **owned all-or-nothing**. `PageRef.notes` is absent while the file's own annotations are merely being displayed, and the first add/edit/delete adopts the *whole* set for that sheet (hence the `baseline` argument on every op); a save then rewrites that page's `/Text` annotations from the list and leaves every other page's untouched. The alternative — tracking per-annotation edits against the file's objects — buys exact preservation of a foreign note's exotic parts (rich text, a reply thread, a custom popup box) at the price of matching annotations across a rewrite, i.e. the bookkeeping that silently duplicates or drops a comment; all-or-nothing is worse in one narrow case, cannot be wrong in the common one, and only ever touches a page the reader actually edited a remark on. `[]` is a real state (every remark deleted) and is why `isPristine` tests `notes` for **presence**, not content. `toPdfDate` writes the format's local-zone stamp — a `Z` would claim a certainty about where the remark was written that we do not have. Also the **walk** the remarks panel is built on: `placedNotes` flattens the arrangement into one list in *reading* order (sheet, then down the page, then across it at a line tolerance, so two remarks pinned beside each other are not swapped by a point of drift) with each remark carrying the **entry id** that owns its sheet — the only shape in which a remark is addressable from outside the page that draws it, and what makes a reorder follow it; `stepNote`/`noteIndexOf` are the ring Next/Previous walk. A sheet the arrangement has taken over contributes its own set, exactly as the page draws it, so the panel and the page cannot disagree about what is on a sheet. |
| `lib/viewers/redact.ts` | Blacking text out of a PDF (#pdf-redact), the pure half — where a mark goes and what it covers; the burn-in that consumes it is `embed/pdf/pdfDoc.ts`'s. Two ideas. **A mark is geometry, not a match**: it is stored in big points in the sheet's rotated space (the space the search hits and link boxes already use), so it follows zoom and rotation for free, and nothing here holds the *text* it covers — the string that motivated a redaction is exactly what must not be carried around. And **snapping only ever grows a box** (`snapToText` is a union, never an intersection): a drag is done by eye at whatever zoom the reader is at, while the burn-in is pixel-exact, so a clipped mark leaves a legible sliver of the very word it was drawn over — the one failure that makes the feature untrustworthy. A drag over a figure, where there is no text to snap to, is kept exactly as drawn. `markMatches` is the fast path (every Ctrl+F hit in one click, from the same measurement the overlay is drawn from) and skips a hit only when an existing mark **contains** it — a box that merely clips one leaves the rest readable. |
| `lib/usageRollup.ts` | Folds UTC day/hour buckets into today/week/month windows. Generic over the payload — shared by `NetworkTrafficPane` (bytes) and the recap (counters). |
| `lib/gpu.ts` | The GPU-memory arithmetic shared by its three surfaces (header row, system monitor, local-model menu), pure; sampling is the backend's `gpustat`. The one idea: **GPU memory is the two pools summed** — the dedicated VRAM carve-out *and* the shared pool mapped from system RAM — because on an APU the carve-out is only the framebuffer (~86% full forever, meaningless) while a model actually lands in the shared half; on a discrete card the shared half is 0 and the sum is plain VRAM. Hence `gpuTone` tones by **ratio**: the old row's absolute 1 GB/2 GB thresholds suited a figure counting only Ollama's models and would peg a device-wide one at "hot" permanently. Also home to the **sensor** formatters (`formatTempC`/`formatWatts`/`formatMhz`/`formatFan`/`gpuLinkLabel`) and the `GpuProc` type: each formatter returns `null` for an unreported reading so the UI omits it rather than print a fake zero, and `gpuAdapterTooltip` appends only the sensors the driver actually gave. The system-monitor pane renders these as a per-adapter GPU card (VRAM + Util meters, sensor strip) plus a once-only per-process GPU-memory list (locally from `gpu_process_snapshot`; for a remote source, from the host-sampled `SystemSnapshot.gpu_procs` in the same poll). |
| `lib/usageMetrics.ts` | The metric keys (mirrors `schema::usage_stats::metric`) + how a tab maps to an agent or a local model. |
| `lib/promptCount.ts` | "Enter with content pending = one submit" — the prompt/command heuristic, fed from `TerminalView`'s `onData`. |
| `lib/conference.ts` | **The** video-call link on an event — pure, and the single verdict every Join button reads (the header's 🗓 dropdown, the event dialog). Two rules. **The field is exact; a sniffed link is a narrow guess**: almost no real invitation carries the link in a dedicated field (Zoom, Teams, Meet and Webex put it in LOCATION or bury it mid-DESCRIPTION), so a link is also derived from those — but only from a **recognized meeting host**, or a location that is *nothing but* a URL. The obvious alternative, the first `https://` in the notes, turns the agenda document and the ticket link into Join buttons, and a button that joins the wrong thing is worse than none: it is clicked in the ten seconds before a meeting, without reading. And **only `http(s)` is ever joinable** — the desktop clients advertise `zoommtg:`/`msteams:`, i.e. launching a local application with arguments out of a file anybody can mail you; the web URL is what all of them also publish. An unrecognized host is named **by its host** rather than "Video call", because where the click is about to go is exactly what a generic word hides. Tested in `src/__tests__/Conference.test.ts`. |
| `lib/caldavPush.ts` | **What bytes a CalDAV resource should contain** — pure, and separate from the invoke surface and the store for that reason. One idea carries it: *a resource is not a row*. CalDAV has no separate occurrence object, so a repeating event's overrides live in the same calendar object as their master and a synced series is several rows sharing one `caldav_href`; `resourceRows` gathers the group, `orderComponents` puts the master first (readers take the component without a `RECURRENCE-ID` as the series definition, and some servers reject a body that opens with an override), and `resourceIcs` serializes them as one body **through `serializeIcs`** rather than a push-specific writer — which is also what keeps board state (`column`/`rank`/`tags`/`subtasks`/`project_id`/`mail`) off the wire, since the file export already excludes it. A row with no href is alone in its resource, whatever else is unsynced on the calendar: grouping on an empty href is how a first push of one appointment uploads five. |
| `lib/calendarWriteHook.ts` | The seam between a calendar edit and CalDAV push: one registered handler, and nothing else. It exists to keep the dependency pointing one way — `stores/caldav` already imports `stores/calendar`, so importing back would be a cycle between two module-scope `create()` calls, the shape that resolves to `undefined` depending on which file the bundler reaches first. The two directions are **not symmetric**: an upsert is announced *after* the local write (an edit made offline is still an edit), a delete *before* it, and a rejection there stops the local delete. One slot, last-writer-wins, deliberately not a listener list — two handlers would mean two pushes of one edit. |
| `lib/icsSafety.ts` | What is *in* an `.ics` file, asked before it is imported — pure, and the only thing between "the user picked a file" and "the calendar changed". Not a scanner and it must not pretend to be: an `.ics` carries nothing this app runs, and the module header lists the four independent reasons (fixed property set into plain data, `stripFormatControls` on every text field, no HTML anywhere in the calendar UI, `lib/conference.ts` refusing every non-`http(s)` link). What it adds is the question those cannot answer *because* they are silent — a `PROCEDURE`/`EMAIL`/`AUDIO` alarm, an `ATTACH`, an application-scheme link, a `METHOD:REQUEST`, a bidi-disguised title, an endless sub-daily `RRULE`, a component kind that is never imported. Each finding carries `ignored`, because "present in the file, discarded on import" and "this reaches your calendar" are different sentences and only two of the seven are the second. A `LOCATION` with no scheme is a room name, not a link — the over-reporting side is the one that makes the dialog worthless. |
| `lib/calendarTime.ts` | Calendar date math: local wall-clock stamps, exclusive ends, overlap layout. `formatTime`/`formatStampTime` are **the** two clock renderers, taking `use24h` from `lib/timeFormat`; both return anything that is not an `HH:MM` unchanged, the empty string included — that is what `timePart` gives for every date-only stamp, and it is the argument every optional-hour call site passes. |
| `lib/timeFormat.ts` | **The one answer to "12-hour or 24-hour?"** (`Settings.time_format_24h`), read by every surface that prints a wall clock: the header clock, the calendar's grids/agenda/tasks, the header's day and to-do lists, the board cards and card dialog, the reminder popup and its OS notification, the mail list's dates, the deck presenter. One setting rather than one per feature, because a clock is not a property of a *feature* — reading 17:00 in the calendar and 5:00 PM on a to-do card two centimetres away is one app disagreeing with itself, which is exactly what the calendar-only switch this replaces did (it moved the grid and left the header clock, the alarms and every task readout behind; the retired `calendar_time_format_24h` is still *read* once as a fallback, never written, so an existing choice carries over app-wide). **Unset derives from the language** — English → 12-hour, the other four → 24-hour — because a default has to be some convention and the honest one is the user's own language, which is a statement they already made; hence `Option<bool>` and never a bare `false`, or a first launch would freeze one hemisphere's habit onto everyone and a later language switch could not correct it. `resolveUse24h` is the pure three-way precedence rule (explicit → retired key → language), `useUse24h()` the hook, `readUse24h()` the same read for a caller outside React — deliberately not named `use…`, since rules-of-hooks lints on the name. It governs the clocks Eldrun *prints*; the one an hour is *typed* into is `common/TimeField`, which had to stop being a native `<input type="time">` for the setting to reach it at all. Tested in `src/__tests__/TimeFormat.test.ts`. |
| `lib/todoBoard.ts` | The to-do board's pure logic — bucketing, ordering, filters, the badge **and its explanation** (`urgentTodos`/`daysLate` — the overdue/today/tomorrow split the header's hover list renders, filtered exactly as `todosDueCount` counts so the number and the rows cannot disagree), the rails' selection, drag geometry. Also the **deadline chip** (`dueDelta`/`dueDeltaKey`), the one place every "3d late" / "in 2h" readout is decided, so the header list and the board card cannot describe one card differently. What it turns on is the *kind* of `due`: a **whole-day** deadline knows only which day, so it is read in whole calendar days at every distance (counted date-to-date, never minutes ÷ 1440 — dividing mixes the time of day into a figure that names a day and makes one deadline read as 2 in the evening and 3 in the small hours) and never in hours, since "in 9h" is a precision it does not have and 00:00 is not what its author meant; a **fixed-hour** deadline is a moment, so the whole distance is given — days *and* hours together past a day (`2d 5h`), hours inside a day, minutes inside an hour, since rolling that to a bare "2d" throws away the half that decides whether the card is this afternoon's problem. `null` where there is nothing worth printing (no deadline, a whole-day one due today, an hour deadline passing this very minute); `dueDeltaKey` keeps the *choice* of phrase here and the wording in `i18n`. Two rules run through all of it. **Completion wins over placement**: `percent`/`completed` are the ICS-round-trippable truth the calendar's Tasks view writes, `column` is Eldrun's own, so a task ticked in the Tasks view is *shown* in Done immediately rather than waiting for the backend to reconcile it — that rule is the only thing keeping the two surfaces from visibly disagreeing. And **nothing here migrates anything**: no render path writes `column`/`rank`, so an unplaced card is merely *displayed* in the first column and acquires a real placement on its first move (writing one at read time would rewrite `calendar.json` once per launch for someone who may only use the calendar). `DEFAULT_COLUMNS` mirrors the backend's seeded set, so the moment the first drag seeds them nothing jumps. Also the **checklist ops** (`mintSubtaskId`/`addSubtask`/`setSubtask`/`toggleSubtask`/`removeSubtask`/`moveSubtask`+`stepDropSlot`) — pure, and shared by the two surfaces that now edit a checklist (the board card inline, the card dialog), which is what keeps one field of one file from having two definitions of what an add or a delete does; the card saves the result, the dialog stages it. Ids are **minted by scanning for a free suffix**, never `${task.id}-${length}`: delete the last step, add another, and the counted spelling hands the new one the dead step's id, after which two id-addressed rows tick and delete each other. None of them touches `percent` — 100% moves a card to Done, so a derived one would relocate the card out from under the pointer that ticked its last step. `moveSubtask` takes an index into the list **without** the moved step — the convention `stepDropSlot` yields and the one place a downward drag lands a row short if the two are mixed. Also the **conversions** (`taskFromMail`/`taskFromOccurrence` over one private `convertedCard`, plus `occurrenceCardOf`): one card shape for both rails — the board's first column, open, unranked, a `created` stamp from the caller's clock, a source line in `notes` and a link (`mail`/`event`) recording what it came from. The source line is glyph + the object's own data and **never a word**, which is what keeps these pure and out of `i18n`: a note written in the UI language of the day would be a fossil of that setting sitting in `calendar.json` forever. Tested in `TodoBoard`/`TodoRank`/`TodoBadge`/`TodoRails.test.ts`. |
| `lib/recurrence.ts` | Recurrence expansion (`expandEvents`) + exdate/override editing. |
| `lib/caldav.ts` | **The** typed invoke surface for CalDAV — one wrapper per `caldav_*` command, the convention `lib/mail.ts` set, and no wrapper takes a path because no command does. It also owns the one piece of real work on this side: `parseChanges` runs each fetched resource's iCalendar text through `lib/ics.ts` (which is why a sync is two commands and not one — a second parser in Rust would be two implementations that can disagree about one feed). Grouping is **by resource, not by component**: one CalDAV resource can hold a recurring master *and* its `RECURRENCE-ID` overrides, and the backend matches rows within an href group. A resource that parses to nothing is still reported, as an empty group — dropping it would present the resource as absent, which a full listing reads as *deleted*. The push wrappers (`caldavPush`/`caldavDelete`/`caldavResourceEtag`/`caldavRefreshAccess`) mirror the same seam outward; `caldavPush` resolving with `conflict: true` is **not** a rejection, because the caller has a question to put to the user rather than an error to report. |
| `lib/ics.ts` | iCalendar parse/serialize (VEVENT/VTODO/VALARM/RRULE), folding + escaping. Carries the video call as RFC 7986 **`CONFERENCE`** (`VALUE=URI;FEATURE=AUDIO,VIDEO`, unescaped — a URI is not a TEXT value), reading Google's older `X-GOOGLE-CONFERENCE` as well, since that is what a Meet invitation actually arrives with. What it deliberately does **not** do is copy a link out of LOCATION/DESCRIPTION into the field at import time: that is a guess, and `lib/conference.ts` makes it at render time where it stays correctable. Two fields round-trip for CalDAV push and are displayed nowhere: **`UID`** (a row goes back to the server as the object it came from — minting `${id}@eldrun` on a push means the server keeps its copy and files ours as a second appointment) and **`RECURRENCE-ID`** (which occurrence an override row replaces; without it a series pushes back as two masters under one UID). `serializeIcs` now also writes a locally-authored series' `overrides[]` as `RECURRENCE-ID` components, which fixes the **file export** too — until then, a series exported and re-imported came back with every moved occurrence silently in its original place. |
| `lib/alarms.ts` | Pure alarm logic: which reminders are due, fire-once keys, snooze. |
| `lib/alerts.ts` | The Alerts group's selectors, all pure — merge mail/events/tasks into one severity-then-time-ordered list. `now` is a **parameter**, not a `Date.now()`, for `todoBoard`'s reason: every interesting case here is a boundary case (a meeting in four minutes, a card due at midnight) and none of them is testable if the clock is ambient. Two window rules and only one is symmetric — the lookahead cuts the future at a **whole day**, since "next week" is a count of days and a rolling 7×24h would show a day-7 event at 08:00 and hide it at 10:00; there is deliberately **no backward limit**, because an overdue thing does not stop being overdue after a fortnight and the group would quietly go green while the deadline is still missed. What bounds the past is the source: a done card and a cancelled event both leave on their own. A **card** is cut again at `TASK_LOOKAHEAD_DAYS` (7, strictly), the shorter of the two windows winning: a meeting next month is a fact about the calendar and belongs in a long lookahead, a card due next month is a *plan*, and a strip of plans is a second board rather than an alert. The already-converted-to-a-card rule is `selectUrgentMail`'s, reused rather than restated, so this group and the board's rail cannot disagree about which marked mail is still asking for something. Muting is here rather than in the component for one mechanical reason: it has to happen **before** the cap (a silenced row that still consumed one of `limit` slots would mute a second thing the user never chose), which is also why the mail pre-cap is `limit + muted.length`. `selectAlerts`/`selectMutedAlerts` are the two halves of one partition of the same feed, and `selectMutedAlerts` returns only rows that are **still live** — the only liveness signal this module has, and why `addMutedAlert` *bounds* the stored list instead of pruning it. Tested in `src/__tests__/Alerts.test.ts`. |
| `lib/calendarCategories.ts` | Event category → color palette. |
